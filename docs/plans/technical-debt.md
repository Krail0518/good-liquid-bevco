| **Fixed** — #297, release-blocking + STILL PRESENT added || **Fixed** — #297, fires at both DST hours || **Partly fixed** — #297 deleted the fake-green jobs; reviewer still not connected || **Fixed** — #310, scoped to client_id is null || **Fixed** — #310, 59 unbacked grants revoked (not 2) || **Fixed** — #309, active-status check added || **Fixed** — #309, 15-min freshness window || **Fixed** — #309, declared from live state || **Partly fixed** — #296 strips at ingestion; template escaping still open |# Technical Debt Register

Per the engineering standard §12. Each item carries severity, affected module,
business impact, recommended fix, and status.

**Source:** a 14-agent audit of `origin/main` on 2026-08-28, run across seven
dimensions (tenant isolation, silent writes, browser storage, injection, edge
functions, test coverage, release automation). Every finding was then given to a
separate agent whose job was to *refute* it. Four were killed outright and five
had their severity corrected; only survivors are listed here.

Severities are the post-verification values. Where the original grading was
wrong, the correction is stated in the notes.

## Status key

- **Fixed** — corrected and verified
- **Applied** — migration live in production
- **Accepted** — owner has accepted the risk; no action planned
- **Open** — not yet addressed

## Where this stands — 2026-08-28 (end of day)

| | |
|---|---|
| Findings recorded | 34, plus 2 from the independent audit |
| Fixed or applied | 24 |
| Merged to `main` | all 14 PRs (#295-#311) |
| Migrations applied AND committed | 6 |
| Edge-function deploys completed | 3 |

Production and the repository now describe each other. Verified after the last
merge: the full invariant sweep passes including the authenticated-identity
probe, the three-identity pricing check holds in both directions, invoice
enumeration returns a uniform miss, and all nine plain-node suites pass on
`main`.

Two things the audit asked for were not code. Both are now done:

- The weekly review calls an independent reviewer (#324). It needs the
  `OPENAI_API_KEY` secret, which only the owner can supply; until then the job
  fails deliberately rather than reporting green.
- The `production-db` environment was created on 2026-08-29 with a required
  reviewer, so the `apply-sql.yml` gate (#298) is enforced rather than merely
  referenced. It had never existed: a workflow naming an environment does not
  create it, so the gate read as protection while providing none.

---

## Fixed / applied

| ID | Sev | Module | Issue | Business impact | Fix | Status |
|---|---|---|---|---|---|---|
| GL-001 | BLOCKER | `src/services/auth.js` | The one-shot localStorage→DB settings migration deleted the local copies without ever writing them to `app_settings`, and set a guard flag so it could never retry. A seeded empty default (`sign_templates '{}'`) also masked three real values. | First admin login after deploy would silently lose the SMS recipient and alert phone, five notification toggles, Dropbox Sign template map, Stripe publishable key and Sentry DSN. Recovery meant re-entering each by hand. | Upsert before delete; abort cleanup on failure so it retries; treat empty seeds as absent. | **Fixed** — `fix/app-settings-data-loss`, 20 checks pass, 12 fail against pre-fix code |
| GL-F1 | BLOCKER | `handle_new_user()` | Signup trigger was a denylist: any auth user without portal metadata got an active `sales` profile. A bare `POST /auth/v1/signup` with the publishable key qualified. | An active profiles row *is* `is_gl_staff()`, which cleared the tenant guard and reached the legacy `USING(true)` policies — full CRUD on every table, `client-docs`, and `link_customer_user_by_email`. Portal customers are competing brands. | Inverted to an allowlist keyed on `auth.users.invited_at`, which the admin API sets and signup cannot forge. | **Applied** — `20260828175051`. Four-branch behavioural test in a rolled-back transaction; no account had used the hole |
| GL-F3 | MEDIUM | storage policies | `compliance-photos` carried `FOR ALL TO authenticated` with only a `bucket_id` test, plus an `anon` SELECT permitting enumeration. | Would give every portal customer full CRUD on FDA evidence — hold tags, defect photos, PCQI-signed documents. | Replaced with a staff-gated policy mirroring `client-docs`; dropped the anon policy. | **Applied** — `20260828180304` |
| GL-XSS | BLOCKER | `src/modules/pipeline/calendar.js`, `booking-confirm` | Day panel concatenated `ev.title` / `ev.notes` into `innerHTML`. Those originate in the public tour-booking form. | A stranger books a tour with an `<img onerror>` name; script runs in a staff session — the origin holding the staff JWT. Same shape as the quote-form incident in `CLAUDE.md`. | Panel rebuilt with `createElement`/`textContent`; angle brackets stripped at ingestion across all 12 downstream uses. | **Fixed** — `security/calendar-stored-xss`. ⚠️ Test has not executed locally; CI is its first run |

**Correction on GL-F3:** first reported as live exposure. It was not — the
`compliance-photos` bucket does not exist (`storage.buckets` holds only
`client-docs`), so both policies were dangling with zero reachable objects.
It was latent, and downgraded accordingly. Hardened before the bucket exists so
it is safe by default.

## Accepted

| ID | Sev | Module | Issue | Business impact | Recommended fix | Status |
|---|---|---|---|---|---|---|
| GL-F2 | — | `profiles` | One account holds both an active staff profile and a `customer_users` portal link, created inside the window when the signup trigger wrongly gave every auth user a staff profile. | None. Owner confirmed 2026-08-28 that the account is his own, used for portal testing — not a customer. | None. Re-check if the account is ever handed to a real client. | **Accepted** |

## Open — security

| ID | Sev | Module | Issue | Business impact | Recommended fix | Status |
|---|---|---|---|---|---|---|
| GL-002 | HIGH | `index.html` | `saveRole` and `deactivateUser` call `renderPermissionsPanel()`, which is IIFE-local in `src/services/permissions-service.js` and never exported. The `ReferenceError` throws *before* the `UPDATE`. | Role changes and deactivations never reach the database. A deactivated staff member can still sign in. Presents as a dead button, not a false success. | Move the render call after the write, or export the function; then adopt the `src/services/auth.js:408` pattern — `.select()`, and treat a 0-row result as failure. | **Fixed** — #300 |
| GL-003 | HIGH | `stripe-checkout-session` | No caller authorization; looks up invoices with the service-role key by guessable sequential invoice number. | Anyone reaching the function can walk the invoice-number space and read every client's amount and payment status. | Require the invoice's `share_token` alongside the number. | **Fixed** — #307 |
| GL-004 | HIGH | `qbo-callback` | The OAuth `state` is read but never validated, and both QBO endpoints are unauthenticated. | The QuickBooks connection can be silently replaced by a stranger. | Validate `state` against a stored nonce; require staff auth. | **Fixed** — #308 |
| GL-005 | HIGH | `.github/workflows/apply-sql.yml` | Can run SQL against the production database from any branch; the path guard never checks the file was merged. | An unreviewed migration can reach production, bypassing the review gate entirely. | Restrict to `main`, require an environment approval. | **Fixed** — #298 |
| GL-006 | HIGH | `booking-confirm` | Interpolated anonymous-caller values into the approval email HTML. #296 added an ingestion strip, but it never covered `booker_email` — that field was only shape-validated by `/^[^@s]+@[^@s.]+.[^@s]+$/`, which excludes just `@` and whitespace. Neither is needed for a payload: `a<svg/onload=alert(1)>@b.co` passes it. | The payload lands in the email carrying the Approve link a staff member clicks. | Escape at the template (`escapeHtml` applied to every value the function did not construct itself), plus an explicit reject of `<>"'` backtick in the address. `stripAngles` stays as defence in depth. | **Fixed** — #318 |
| GL-007 | MEDIUM | `stripe-webhook` | Requires `verify_jwt=false`, but neither deploy path configures it. | A routine redeploy silently breaks payment reconciliation. | Set it in `config.toml`. | **Fixed** — #309, declared from live state |
| GL-008 | MEDIUM | `mailgun-webhook` | Signatures are verified but the timestamp is never checked. | Captured callbacks are replayable indefinitely. | Reject timestamps outside a short window. | **Fixed** — #309, 15-min freshness window |
| GL-009 | MEDIUM | `delete-staff-user` | Hand-rolled caller check omits the inactive-account guard. | A deactivated super-user can still hard-delete auth accounts. | Use the shared `requireStaff()` helper. | **Fixed** — #309, active-status check added |
| GL-010 | MEDIUM | migrations | `capacity`, `case_studies`, `resources` still carry the 2026-05-18 `USING(true)` policy and sit outside the tenant guard. | Any authenticated user can edit or delete published marketing content. | Drop the three `authed all` policies, or add the tables to the guard. | **Applied** — 20260828221639, #306 |
| GL-011 | MEDIUM | migrations | `anon` holds `SELECT` grants on `formulas` and `yield_logs` with no policy justifying them. | Inert today, but the formula vault sits one stray policy from public — the exact shape of the 2026-05-18 incident. | `revoke select on public.formulas, public.yield_logs from anon;` | **Fixed** — #310, 59 unbacked grants revoked (not 2) |
| GL-012 | MEDIUM | `gl_tour_intake_submit` | The `UPDATE` is scoped by email alone and does not exclude rows carrying a `client_id`. | An anonymous caller who knows a client's contact email can overwrite that client's stored formula/ingredient answers. | Add `and client_id is null` to the `WHERE`. | **Fixed** — #310, scoped to client_id is null |
| GL-013 | LOW | migrations | `20260721_tour_alerts.sql` passed the literal `'gl-notify-2026-abc123'` to `vault.create_secret`. That value authenticated the database's own triggers to the `notify-deal` edge function, and it was committed — anyone with repo read access held the credential. | It is in git history permanently and cannot be un-leaked, only rotated. Production was rotated on 2026-07-30 by `20260730002000` and the live value was confirmed not to equal the literal, so the exposure is closed. The file still mattered: migrations get replayed, and a fresh environment would re-create the published credential. | Migration generates the value in-database and raises if the published literal is ever live. Two guards at different layers: `tests/committed-secrets.test.cjs` keeps literals out of migrations, and `SECRET` facts in the drift snapshot report whether production still accepts a known-published value — recording only a length and a boolean, never a value. | **Fixed** — #316 |

## Open — data integrity

| ID | Sev | Module | Issue | Business impact | Recommended fix | Status |
|---|---|---|---|---|---|---|
| GL-020 | HIGH | invoicing | Every "mark invoice paid" path writes AR status without `.select()` or a rows-affected check; one has no error handling at all. | RLS rejects silently — the UI reports success while nothing saved. `CLAUDE.md` attributes ~40 bugs to this class. | Append `.select()`; treat `error` **and** an empty array as failure. | **Fixed** — #301 |
| GL-021 | HIGH | `index.html` | Six delete paths check only `.error`, never rows-affected. | Deletes report success, purge the local cache, and write a false audit entry. | Same pattern as GL-020. | **Fixed** — #302 |
| GL-022 | HIGH | pipeline / clients | Deal, referral, referrer and client creation fall back to a browser-only synthetic id when the INSERT fails. | Business records exist only in that tab and vanish on reload. | Fail loudly instead of synthesising an id. | **Fixed** — #303 |
| GL-023 | HIGH | compliance | "Reset CCP limits to defaults" clears only the local cache; the DB override still wins. | FDA critical limits are never actually reset. | Delete the DB override. | **Fixed** — #305 |
| GL-024 | HIGH | admin tools | "Clear local cache" offers pre-checked deletion of legacy blobs that are still the only copy, labelled "safe to wipe". | Silent business-data loss initiated from a UI that says it is safe. | Migrate the blobs, or remove them from the default selection. | **Fixed** — #305 |
| GL-025 | ~~MEDIUM~~ | `src/shared/tools.js` | ~~Service package pricing lives only in per-device localStorage.~~ **Withdrawn — not a defect.** The finding was read off the stale `C:\Users\mike\Desktop\goodliquid` snapshot, which still has `gl_service_packages` in localStorage. This repo moved the catalog to `app_settings.service_packages` in `921eef0`, and `src/shared/tools.js` has used `glGetSetting`/`glSaveAppSetting` ever since. | None. | None needed. Verified against the real repo on 2026-08-29. | **Withdrawn** — audit artifact |
| GL-026 | MEDIUM | CIP audit | A rejected DB write leaves an FDA-required CIP record in `gl_cache_compliance_records`, but the CIP page read `gl_cip_logs` — a key nothing has ever written, because its only writer `saveLocal()` was never called. The fallback was therefore unreachable, and the page rendered "No cycles logged yet". | The operator cannot tell a sanitation record that failed to file from a cycle nobody logged. | Read the key the writer actually writes; show unsaved cycles with a retry that checks rows-affected. | **Fixed** — #314 |
| GL-027 | MEDIUM | compliance | `public.cip_logs` exists with RLS but holds 0 rows; the 72 real CIP records live in `compliance_records` under `GMP-SAN-002`. An error message pointed debuggers at the empty table, and `auditor.html` made three fetches whose results were never read. | Wasted debugging time; ambiguity about the system of record; three dead round trips per auditor page load. | ADR-0001 names `compliance_records` the system of record; dead readers removed. Dropping the table itself is separate — it changes the authorization surface. | **Fixed** — #314 |

## Open — architecture and process

| ID | Sev | Module | Issue | Business impact | Recommended fix | Status |
|---|---|---|---|---|---|---|
| GL-030 | HIGH | `.github/workflows` | The weekly AI review was an echo-only placeholder; `ci.yml` and `security-scan.yml` were unconditionally-green jobs running under trustworthy names. | The review gate the standard describes did not exist in executable form, while appearing to. | #297 removed the fake-green jobs. #324 connects the reviewer: `scripts/ai-review.mjs` calls an **independent** model (OpenAI, not Anthropic — `AGENTS.md` splits the roles so the reviewer did not write the code), and `scripts/ai-review-file-issues.mjs` files each finding as an issue carrying all six fields Setup Instruction #1 requires, commenting on the existing issue rather than duplicating a recurring one. Every "did not actually review" path — missing key, HTTP error, unparseable output, no findings array — **fails** rather than reporting zero findings. | **Fixed** — #324, #331; needs `OPENAI_API_KEY` (preferred) or `ANTHROPIC_API_KEY` (accepted, same family as the implementer, and every run and issue says so) |
| GL-031 | HIGH | deployment | Nothing prevents production deploy when the gate fails — Vercel auto-deploys on push to `main`. | A failing review cannot stop a release. | Branch protection + required status checks. | **Fixed** — #313; `main` requires `smoke`/`invariants`/`drift`, strict. `enforce_admins` off and no required reviews, both deliberate and reasoned in `docs/runbooks/deployment.md` |
| GL-032 | HIGH | `scripts/security-invariants.sh` | Only ever authenticates as `anon`; never tests the `authenticated` role. | Cannot catch the 2026-05-18 class of hole it was written for. `CLAUDE.md` asks for all three identities. | Add a section signing in as a throwaway self-registered account, asserting zero rows from `clients`, `invoices`, `formulas`. | **Fixed** — #299 |
| GL-033 | HIGH | `tests/full-sweep.cjs` | Scores a missing global as PASS. | A module that stops loading turns 18 checks green. | Fail on absence. | **Fixed** — #304 |
| GL-034 | MEDIUM | tests | The `pageerror` noise filter swallows genuine app crashes — `Maximum call stack` was listed as expected noise, and substring matches on `supabase`/`status of 4` discarded null-derefs that merely named a library. | Guts the "no fatal JS error" assertion in every browser test. | `ALWAYS_APP_BUG` checked before the noise list, in both suites; `test-integrity` drives the real regexes. | **Fixed** — #313 |
| GL-035 | MEDIUM | `.github/workflows` | The Tuesday deep-review cron `0 12 * * 2` is 08:00 EDT but 07:00 EST; no timezone configured. | Drifts an hour every winter. Setup Instruction #1 explicitly requires an intended timezone. | Run at `0 11,12 * * 2` and exit unless the local New York hour is 8. | **Fixed** — #297, fires at both DST hours |
| GL-036 | MEDIUM | `.github` | The AI-review issue template omits the release-blocking field and the STILL PRESENT re-review state; the PR template omits 7 of the 15 §15 checklist items. | The remediation loop has no field to gate on. | Add the missing fields. | **Fixed** — #297, release-blocking + STILL PRESENT added |
| GL-037 | MEDIUM | architecture | 305 top-level functions in one ~9,300-line inline script inside `index.html`, plus 55 `crm-*.js` files at the repo root. | §3 names this directly. Highest-blast-radius area for unintended effects. | **Done.** #321 lifted the inline block into `crm-index-core.js`; #325–#329 extracted 32 capabilities from it; #341–#348 relocated the remaining 55 root files in seven risk-ordered groups. **86 modules** now under `src/modules/<domain>/`, `src/services/` and `src/shared/`; `crm-index-core.js` is the only `crm-*.js` left at the root, as the entry point. Every move byte-identical with script order compared index-by-index. Guards pin the taxonomy, load order, the classic-script contract, that every module on disk is loaded, and that no second root module appears. The migration also surfaced three live defects the tests could not previously see: an unchecked meeting-note delete and a blind delete/insert pair on `user_permissions` (both GL-021 class). | **Fixed** — #321, #325–#329, #341–#348 |
| GL-038 | LOW | product | The `compliance-photos` bucket had never been created, so hold-tag photos, defect photos and PCQI document signing all failed. The UI told staff to create it in the Supabase dashboard — the change CLAUDE.md rule 2 forbids. | A documented compliance feature did not work. | Created **private** by migration, matching `client-docs` and `sales-decks`; a public bucket would serve FDA evidence to anyone holding a guessable URL. Client moved off `getPublicUrl()` to storing the path and signing on display. | **Fixed** — #315 |
| GL-039 | MEDIUM | tests | Four test files existed in `tests/` but were referenced by no workflow, so they never ran: `test-integrity`, `cip-system-of-record`, `formulation`, `training-edit`. One of them was the guard written for GL-034. | Same failure mode as GL-034 — the suite credits itself for coverage it is not providing, and the file passing by hand hides it. | All four wired; `test-integrity` now fails if any test file is referenced by no workflow. Wiring `formulation.test.cjs` immediately caught GL-041, a live data-integrity bug it had been silently guarding against all along. | **Fixed** — #314 |
| GL-040 | LOW | client-wide | 19 `localStorage.getItem` keys had no literal `setItem`. **Triaged: no defects.** 13 are one-way legacy migration readers guarded by a `_migrated` flag; `gl_mailgun_key` and `gl_customer_logins` are read only in order to `removeItem` them (deliberate cleanup of legacy secrets); `gl_sms_to_phone` is an alias fallback whose primary key `gl_sms_alert_phone` **is** written; `gl_active_timer` is a legacy reader explicitly listed as transient. `gl_mailgun_from` is dead config where the hard-coded default always wins — harmless. | None. The scan counts an upper bound, not defects: it cannot see writes through variable keys, and a one-way legacy reader is correct by design. | Closed by triage. The one genuine problem it surfaced is tracked separately as GL-043. | **Closed — no defect** |
| GL-043 | HIGH | `src/modules/production/compliance-ext.js` | `getCurrentUserId()` read `window.__GL.session.user`. **Nothing in this codebase ever assigns `window.__GL`** — it is read in two places and written in none — so the function returned `null` on every call. Every other user lookup in the same file already used `window.currentUser`. | The null fed `inspector_tokens.created_by` (who granted an external FDA auditor access to compliance data) and `compliance_records.second_signed_by` (the dual-PCQI co-signature, whose only other field is free text typed at a `prompt()`). A co-signed FDA record was attributable to nobody. Latent: 0 co-signed records and the one existing token came via another path. | Read `window.currentUser` first, keep `__GL` as a fallback; refuse the co-signature outright when the signer cannot be identified. Deleted the dead `getCurrentUserName()`, which read the same never-set property and fell back to a hard-coded person's name. | **Fixed** — #319 || GL-041 | HIGH | `src/modules/pipeline/formulation.js` | `glLoadFormulators()` set `CACHE = CACHE || []` both when Supabase was not ready and when the fetch threw. `[]` is truthy, so the `if(CACHE && !force)` guard short-circuited on every later call and the house list stayed empty for the rest of the session. One early call — the warm-up poll, or any form binding before the client is up — was enough to poison it. | The formulator dropdown never seeds, ticking "formulation done" cannot auto-pick a house, and the deal saves `formulation_vendor: null`. Formulation revenue is attributed to no one. Deals are affected; clients happened to escape only because adding a formulator forces a refresh first. | Only cache a load that reached the server; leave `CACHE` null so the next call retries. | **Fixed** — #314 |
| GL-044 | HIGH | `index.html` | `logoutCRM()` and `saveReferrer()` were each declared twice at top level in the same inline script. Two `function` declarations of one name do not conflict — the **last silently wins for every call site**, including the Sign out button, which appears textually above both. For `logoutCRM` the winner was the correct one (calls `supa.auth.signOut()`, resets `crmInited`); the dead copy cleared local state only. | Sign-out ended the Supabase session **by accident of declaration order**. GL-037 extracts this block one capability at a time, which reorders code by definition — moving the live copy above the dead one, or extracting one and not the other, would have silently reverted logout to "clears the screen, keeps the session", with nothing failing and nothing visible in review. | Both dead copies removed. `tests/duplicate-declarations.test.cjs` fails on any repeated top-level name, and asserts the surviving `logoutCRM` still signs out. Treat as a precondition for GL-037. | **Fixed** — #320 |
| GL-042 | HIGH | `index.html`, `documents` | The Documents page had never worked, for three stacked reasons. `saveDocument()` inserted `file_url` and `file_path`; **neither column existed** on `public.documents`, so PostgREST rejected every insert with PGRST204. The value being stored was a `getPublicUrl()` on the **private** `client-docs` bucket, which returns HTTP 400. And `renderDocs()` drew a delete button but **no download control at all**, so even a saved document was write-only. | Production held 0 rows. Each attempt uploaded the file first, leaving it in the bucket with nothing pointing at it. | Added `documents.file_path` by migration (with a guard refusing a `file_url` column); the uploader returns a path; the page signs on demand and offers a Download. The insert now uses `.select('id')` and treats 0 rows as failure. | **Fixed** — #317 |
| GL-DEF-01 | MEDIUM | `vercel.json` | The CSP allows `'unsafe-inline'` in `script-src`, because **550** inline `on*` handlers across 48 files resolve against `window`. | Weakens the main defence-in-depth control against injected script. The escaping rules in CLAUDE.md rule 5 are what actually stand between a lead-supplied string and a staff session. | **Phases 1–6a done — #350–#356.** `src/shared/actions.js` is a delegated dispatcher resolving `data-gl-action` against an **allowlist registry**, not `window[name]`, so a converted control reaches only what was registered. Arguments travel as `data-gl-arg1="${esc(v)}"` read via `dataset` — not JSON-in-attribute, which rule 5 forbids; proven with 10 hostile payloads in a real browser before the file was written. Phase 2 converted the 52 pure-close handlers to `data-gl-close`, verified by clicking all nine close paths in a real browser. One was refused: `this.closest(div[style*=fixed])` needs `closest` semantics rather than `querySelector`, and extending the dispatcher for a single site is worse than leaving it. `tests/inline-handler-budget.test.cjs` is the ratchet — a per-file budget that fails if a count goes UP or if a budget is left with slack. Phases 2–7 in the plan: close buttons + hover CSS (65), `index.html` (234), core (73), 45 modules, the 145 needing extraction, then drop `unsafe-inline`. | **Fixed** — #355, #357, #386. 550 → 0 handlers; `script-src` no longer carries `'unsafe-inline'`, verified enforcing in production. This row sat at "phase 6a of 7" long after the work finished. |
| GL-045 | INFORMATIONAL | `public.capacity` | The one remaining anon policy with `USING (true)` — the shape CLAUDE.md rule 1 forbids. Found during the end-of-work verification sweep, not by a test. | **Not a leak.** The table holds `quarter`, `booked`, `cans_capacity`, `bottles_capacity`, `week_start` — no client identifier — and the public marketing site reads it. Every other anon policy is scoped (inspector-token gated, `published = true`, `is_active = true`, or insert-only public forms). | Left permissive on purpose; scoping it would break the public site for no gain. But its safety is an assumption about the schema, and CLAUDE.md's central lesson is that assumptions expire silently — so section 2b of `security-invariants.sh` now probes it from outside with the anon key and **fails if the table ever gains a client-identifying column**. | **Closed — enforced assumption** |
| GL-046 | HIGH | `supabase/functions/*` | **18 edge functions import `https://esm.sh/@supabase/supabase-js@2` — a floating major.** esm.sh resolves it to whatever the newest 2.x build is at request time (2.112.4 today). The code that runs tomorrow is not the code that was reviewed today, and nothing in the repo changes when it moves. | **All 18 hold `SUPABASE_SERVICE_ROLE_KEY`**, which bypasses RLS completely. Whatever that URL serves runs with unrestricted read/write on every client's data — the outcome CLAUDE.md names as the worst this system can produce. No off-the-shelf scanner would ever report it: there is no package.json, so `npm audit` has nothing to read and Dependabot sees only the workflow files. | Guard added first: `tests/supply-chain-pinning.test.cjs` froze the count so it could not grow. All 18 then pinned to `@2.112.4` — **the exact build esm.sh already served for `@2`**, verified by `X-Esm-Path` resolving to a byte-identical path, so the change is behaviour-preserving by construction. The test baseline is now **zero**: any reintroduced floating specifier fails CI. Deployed with `gh workflow run "Deploy Supabase"`, since edge functions do not ship on merge. | **Fixed** — #364 |
| GL-047 | MEDIUM | `index.html:3228` | The Chart.js `<script>` tag pulls from `cdn.jsdelivr.net` with an exact version but **no Subresource Integrity attribute**, and the CSP `script-src` allows that origin. | A CDN compromise or a swapped file at that URL executes arbitrary script in staff sessions — the same reach as the stored-XSS class CLAUDE.md rule 5 exists to prevent, just entering through a different door. Version-pinning alone does not help: the pin names a path, not a file's contents. | Added `integrity="sha384-e6cc9LaI…" crossorigin="anonymous" referrerpolicy="no-referrer"`. The hash was taken from the file jsDelivr actually serves and confirmed **three ways**: two independent fetches, plus a vendored copy then present in `index_files/` (since deleted as dead weight) — all byte-identical (205,488 bytes). Verified in a real browser rather than assumed, because a wrong hash blocks the script site-wide: `Chart` loads as **4.4.2** and renders a chart, while an injected tag carrying a deliberately wrong hash is **blocked**, proving the browser enforces SRI here rather than ignoring the attribute. Test baseline is now zero. | **Fixed** — #366 |
| GL-051 | LOW | `src/modules/customers/requests.js`, `src/shared/help-features.js` | The public marketing site opened **two staff-only realtime channels** (`customer_requests`, `invoices`) and polled `customer_requests` every 60s for visitors who were not logged in. | Not a data leak — RLS refuses all of it, which is exactly why it stayed invisible. It was wasted work on every public page view: two WebSocket subscriptions and a poll timer running for someone reading the marketing copy, plus console noise that makes a real error harder to spot. | Both gated on `window.currentUser`. **Polled, not checked once** — these modules run at DOMContentLoaded and sign-in happens later with no page reload, so a single check would have silently killed the paid-invoice notification. That global was the open question: I could not tell by reading whether the staff login path sets `window.currentUser` or the module-scoped copy in `crm-index-core.js`, and guessing wrong breaks a working feature quietly. Mike ran a read-only console snippet on a live admin session, which showed `windowCurrentUserSet: true` with both channels joined. Both sides then verified: anonymous load goes 2 channels to **0**, and setting the global the way sign-in does brings both back within the poll interval. The banner refresh also re-checks per call, so polling stops at sign-out too. | **Fixed** — #391 |
| GL-048 | MEDIUM | CI | The repository had **44 named CI steps and zero scanners** — no secret scanning, no dependency scanning, no `dependabot.yml`. CLAUDE.md rule 6 ("secrets never live in `crm-*.js`, `index.html`, or a migration") was enforced by attention alone. | A committed secret would have been found by whoever noticed it, which is the same control that failed for the dashboard-applied anon policies. | Added `.github/workflows/security-scan.yml` (gitleaks, pinned to 8.30.1 and checksum-verified) plus `.github/dependabot.yml` for the one manifest that exists. History was scanned by hand first — 1,414 blobs, zero provider keys, one JWT with `role=anon` (public by design) — so the job did not switch on over a pile of pre-existing findings everyone would learn to ignore. The allowlist is itself guarded: an anon and a `service_role` token for this project share a byte-identical base64 prefix for 73 characters, so the obvious allowlist would have hidden the one finding that matters. | **Fixed** — #359 |
| EXT-002 | HIGH | `src/services/auth.js` | A null profile lookup fabricated `{role:'sales', status:'active'}` and logged the user in. `_glFetchProfile` returned null on a **missing row, a failed query, OR a thrown error** — all three landed there. Supabase signup is open, so a self-registered stranger was handed an active salesperson identity and shown the CRM shell. | The database was never fooled — RLS refuses those queries, which is exactly why the production invariant probe kept passing and nothing looked wrong. But that is the second line of defence doing the first line's job, and it contradicted `20260828175051_staff_profile_requires_invite.sql`, which had just made the same rule fail closed in the database. | Fails closed: no row, no role, or a non-active status denies the login and signs the session out. A **deny**-list not an allow-list — an allow-list of the roles I could find would lock out any role I had not seen, and `customer_users` is constrained to exactly ('owner','member') so the portal roles are knowable. `tests/auth-fail-closed.test.cjs` covers six denial paths plus the success path, so the guard cannot be satisfied by simply breaking login. | **Fixed** — #378 |
| EXT-028 | HIGH | `sw.js` | The service worker cached **every** successful GET with no origin, status or type check — including cross-origin Supabase REST responses holding one client's invoices, formulas and documents. | Those responses sat in the browser Cache API, survived logout, and would be served to whoever used that browser profile next. On a shared machine that is cross-tenant disclosure — the outcome CLAUDE.md names as the worst this system can produce. None of the 39 test files touched the service worker. | Cross-origin requests are no longer intercepted at all (no `respondWith`, so no code path can cache them). Two things were **not** obvious and were found only by testing: renaming the cache does **not** reliably evict what the old worker stored, and the browser kept running the previous `sw.js` from its HTTP cache so the new purge never ran — `updateViaCache:'none'` was what actually fixed it. | **Fixed** — #377 |
| EXT-005 | MEDIUM | `supabase/functions/{ai-proxy,mailgun-send,send-sms,dropbox-sign}` | Four endpoints that call paid vendors had authorization but no rate limit. | Rule 7 was already satisfied — all four require a staff session, so a stranger cannot reach them. But authorization decides *who* may call and says nothing about *how often*, and these are the four endpoints where "how often" has an invoice attached: Anthropic tokens, Mailgun sends, Twilio messages, Dropbox Sign envelopes. A stolen staff session could loop any of them. | New `public.edge_rate_limits` + `gl_rate_limit_hit()`, fixed windows, service_role only, RLS with no policy. **Fails open by design**: a limiter that takes email and AI down when a table blips does more damage than the abuse it prevents, and the abuse case needs a compromised account first. | **Fixed** — #384 |
| EXT-025 | MEDIUM | 8 modules | Eight `.update()` calls checked `r.error` and stopped there, then updated local state or told the user it worked. RLS refuses silently — zero rows, no error. | Worst was `portal-public.js:212`: on a silent refusal it still returned a link built from a share token the database never stored, so the customer received a URL that could not resolve and nothing reported a problem. | `.select()` plus a row-count check on all eight. **Triaged rather than swept** — 26 unchecked calls exist, but 18 assert no success at all (fire-and-forget AI brief state, telemetry), and bolting alerts onto a background touch is noise pretending to be rigour. One stays by choice: `compliance-ext.js:669`, whose `return true` means "token valid", not "write landed". | **Fixed** — #383 |
| EXT-012 | MEDIUM | `supabase/functions/booking-confirm`, `public.bookings` | The conflict check and the insert were separate statements with nothing between them, so two public requests for the same slot could both pass the check. | Two ordinary visitors clicking the same slot in the same second is not adversarial, it is Tuesday. Note the audit was **wrong** that throttling was absent — 3/email/day and 20/hour exist; the limit is written as plain counts rather than anything named like one, and I nearly repeated the error. | An **exclusion** constraint, not a unique key: slots are ranges and the failure is overlap, so 2:00–2:30 and 2:15–2:45 collide on no single column. The predicate covers only statuses that occupy a slot — `declined` is in live use and must stay free to overlap. `booking-confirm` turns 23P01 into a 409 with "that time was just booked" rather than a 500. | **Fixed** — #382 |
| EXT-030 | LOW | `supabase/functions/booking-availability` | Public, unauthenticated, called Google Calendar on every request, and its date was format-checked but never range-checked — `9999-12-31` was a valid query. | Anyone could walk dates indefinitely and spend Mike's Calendar API quota, the one resource here metered by somebody else. Confirmed the endpoint returns only `{time, available}` — **the audit's claim that it exposes personally identifiable calendar data is false**. | Dates outside a real booking window (yesterday to +365 days) are answered from the request and never reach Google. Deliberately not a rate limiter: it removes the incentive rather than counting requests, and needs no state. | **Fixed** — #382 |
| EXT-024 | LOW | `crm-index-core.js`, `invoice-builder.js` | Invoice numbers were computed in the browser by two different generators — `max(loaded)+1`, and `'GL-' + year + '-' + Math.floor(Math.random()*9000)` in a format no stored row uses. | **Smaller than first reported, and the correction matters.** I claimed a duplicate could make Stripe charge the wrong invoice. `invoices_invoice_number_key UNIQUE` **already exists in production** — created by no file in `supabase/migrations/`, which is why grepping the directory missed it. Duplicates were never possible; a collision produced a failed save, not a bad charge. | `gl_next_invoice_number()` allocates under `pg_advisory_xact_lock`, staff-gated. Called at SAVE time, not render — `#ginv-id` is filled when the builder opens and can be minutes stale. Falls back to the local computation if the RPC fails, with the constraint as backstop: losing the ability to invoice would be worse than a rare retry. | **Fixed** — #380 |
| EXT-032 | MEDIUM | `billing-admin.js`, `password-change.js` | JSZip and jsPDF were injected with `document.createElement('script')` from a CDN with **no integrity attribute**. | A script injected from JS is exactly as privileged as one in the markup. **My own supply-chain guard could not see them** — it scanned static `<script src>` tags and reported "0 without SRI" the entire time, while the Chart.js tag beside them had one. | SRI added, hashes taken from the served files and confirmed stable. The guard now walks `.src` assignments too, matching on the **variable** rather than proximity — a line-window version produced a false positive on my own fix, because a comment pushed `.integrity` seven lines down. | **Fixed** — #379 |
| EXT-029 | MEDIUM | `portal-customer.js`, `password-change.js` | Temporary passwords built from `Math.random()`, which is not a cryptographic RNG. | One of the three is **emailed** as a reset password, so a predictable value there is directly usable by anyone who knows roughly when a reset happened. | `crypto.getRandomValues` with rejection sampling — 256 is not a multiple of the 57-character alphabet, so a bare modulo would bias the first characters. The `GL!`/`aZ1` affixes are kept deliberately: they guarantee the complexity rule the auth provider applies. | **Fixed** — #376 |
| EXT-031 | LOW | `compliance.js`, `crm-extras.js` | Neither CSV escaper neutralised formula prefixes. A cell beginning `=`, `+`, `-` or `@` is read by Excel as a **formula**, and these exports carry lead, customer and staff free-text. | Quoting does not help — the quotes are consumed by the CSV parser and the formula survives, which is why both escapers looked correct and were not. | A leading `-` is only neutralised when what follows is **not a number**: invoice amounts and credits are legitimately negative, and prefixing `-25.00` would turn every negative figure into text and break the sums the export exists for. Both copies fixed — fixing one would have left the other exporting the same data unguarded. | **Fixed** — #381 |
| EXT-035 | LOW | `SECURITY.md` | The document still showed `script-src` with `'unsafe-inline'`, explained why it was "required", and listed its removal as future work. The secret table pointed at `fix.js` for a key that is not there and names the wrong key type. | A security document that overstates enforcement is what an auditor, a customer or a future maintainer reasons from. The audit that prompted this work made exactly that mistake at a larger scale; leaving our own doc stale would be the same error committed deliberately. | Corrected to describe what is actually enforced, what it took (134 handlers, 6 inline blocks, 29 `javascript:` URLs), which guards keep it true, and that `style-src` still carries the allowance and why that is separate. | **Fixed** — #385 |
| EXT-036 | MEDIUM | `compliance.js`, `compliance-ext.js` | Five Print/Close buttons in the compliance report popups were dead. The views are written into `window.open('','_blank')`, and the dispatcher listens on the **main** document — the popup has no listener at all. | **A regression I introduced.** GL-DEF-01 phase 5 (#355) converted `onclick="window.print()"` along with everything else; converting markup rendered into a *different document* was wrong. It failed silently because the dispatcher that would log "no action registered" is not in the popup either — and `close` hid it further, since `glRegisterGlobalActions` resolves names against `window` and **`window.close` is a real method**, so the registry check could never flag it. | Restoring the inline handlers is impossible — a `window.open('')` document inherits the opener's CSP, which no longer permits inline script. The opener binds them instead, so no script runs in the popup. `inline-handler-budget.test.cjs` now fails if a file writes `data-gl-action` into a popup without binding it, scoped to the region between `window.open()` and the matching write after a first version produced three false positives. | **Fixed** — #386 |
| GL-DEF-02 | MEDIUM | `vercel.json`, 6 pages, 16 modules | `style-src` allowed `'unsafe-inline'`, which covers both style **elements** and style **attributes**. 28 style elements existed: 7 inline `<style>` blocks in the pages, 10 built at runtime with `document.createElement('style')`, and 11 generated into print/report popups. | The two halves are not equally dangerous, and that is the whole reason to separate them. A `<style>` ELEMENT can carry attribute selectors — `input[value^="a"]{background:url(...)}` — which is the primitive behind CSS keylogging and CSS exfiltration. A style ATTRIBUTE applies only to its own element and cannot select anything. Leaving both under one permissive directive meant the dangerous half was open in order to keep the harmless one working. | All 28 moved to stylesheets, then the directive split: `style-src-elem 'self'` strict, `style-src-attr 'unsafe-inline'` unavoidable (~6,000 attributes, and no nonce can ever cover a style attribute), plain `style-src` left permissive as the pre-CSP3 fallback for Firefox <122. CSS was **evaluated** out of the source rather than retyped, since much of it is multi-line concatenation. Two needed hand work: the invoice PDF interpolated `inv.status` into the badge colours (now modifier classes) and the public invoice view is not a popup at all. One race fixed: that popup printed on a bare 350ms timeout, harmless when the CSS was inline, a coin-flip once it became a `<link>`. Verified by serving the real CSP locally — an inline `<style>` stops applying, a linked one applies, a `style=` attribute still applies. | **Fixed** — #389 |
| GL-050 | LOW | `src/shared/help-features.js`, `crm-index-core.js`, `crm-extras.js` | Three fire-and-forget calls to promise-returning Supabase APIs — `removeChannel()` and two `signOut()` — were wrapped in `try/catch`, which cannot catch a **rejected promise**. supabase-js 2.106.2 rejects out of its channel-leave path with `RangeError: Maximum call stack size exceeded`. | Measured on production, not inferred: a **returning** visitor (service worker registered, cache warm) reliably got ~18 unhandled rejections during page init. A cold browser got none, which is why it had gone unnoticed. `fix.js` pipes every `unhandledrejection` to `Sentry.captureException`, so for logged-in staff this is quota spent on noise that also buries real errors. Reproduced deliberately by calling `removeAllChannels()` on a joined channel. | The promise is handled where it was previously dropped. Deliberately silent and still not awaited — a failed channel-leave is not actionable (the channel is rebuilt on the next line) and logout must not depend on a network round-trip. I first assumed this was an unbounded retry loop and **it is not**: the channels reach `joined` and the errors stop, which the fix note records so nobody re-derives it. | **Fixed** — #390 |
| GL-052 | LOW | `compliance`, `facilities`, `formulators`, `lead-followups`, `invoices` loaders | Staff-only tables were queried at init for logged-out visitors. Measured by patching `fetch` before any page script ran and capturing a stack per request, rather than grepping: **nine** loaders, not the six the 401 count suggested — several tables answer 200 with `[]` under RLS and so never showed as 401s. One was a `setInterval`, polling `compliance_records` forever in the background of the public marketing site. | No exposure — RLS refuses every one, which is exactly why it stayed invisible. It is waste on every public page view plus console noise that buries real errors. | New `window.glWhenStaff(fn)` in `crm-index-core.js` (loaded 3rd, before every module) defers a callback until `window.currentUser` exists, polling rather than checking once — a one-shot check is the trap that nearly shipped in GL-047, because sign-in happens later without a page reload and would silently disable the feature for everyone. Applied to nine init sites; five render functions also guard internally, since a MutationObserver re-triggered them independently of boot. **Logged out: 6 → 0 401s, and only 3 queries remain, all genuinely public** (`capacity`, `case_studies`, `resources`). **After sign-in: 21 tables reached**, so nothing was disabled. `training-edit.test.cjs` needed a staff session added — it had been rendering staff UI with no session, relying on the behaviour being fixed here. | **Fixed** — #394 |
| GL-049 | MEDIUM | `public.invoices` (and likely its siblings) | `updated_at` was not maintained on **21 of the 42 tables that have the column** — including `invoices`, `clients`, `deals`, `quotes`, `referrals` and `tasks`. There was no trigger, and most write paths never set it, so the column only moved when application code happened to set it by hand. | A column named `updated_at` that does not track updates is worse than no column, because every reader assumes it does. Delta syncs, "recently changed" views and any audit question about when an invoice was altered all get a confident wrong answer. Observed live: GL-1024 went `pending` → `paid` with its timestamp frozen two hours back, GL-1015 changed status while still reading 13 July. | Attached the **existing** `public.set_updated_at()` to the 21 unwired tables — no new function, closing a gap in a convention the schema already had (20 tables were already wired). Idempotent, skips anything already maintained. Reproduced before (`timestamp_moved: false`), verified after (`true`), every probe inside a transaction that rolls back so nothing was written and realtime emitted nothing. All three identities checked: staff writes still work and now stamp; a portal customer still sees exactly 1 client and 0 staff profiles; a self-registered stranger still sees nothing. Coverage went 21/42 → **42/42**. | **Fixed** — #393, migration `20260830210000_maintain_updated_at` |
## ID collision, 2026-08-30 — read this before citing GL-046/047/048

Three rows were logged under IDs that were already taken. The next free number
was picked from a stale read of this file instead of from the file as it stood,
and GL-046, GL-047 and GL-048 had all been used earlier the same week.

Two produced duplicate rows. GL-047 was worse: the edit matched the existing row
and REPLACED it, so **the Chart.js SRI entry (#366) was destroyed rather than
duplicated** and would have vanished from the register entirely. It has been
recovered from git and is back in place.

The three new rows are renumbered. The pull requests are already merged and
their commit messages still carry the old IDs, so the mapping is:

| commit says | actually | subject |
|---|---|---|
| GL-046 (#390) | **GL-050** | three dropped Supabase promises |
| GL-047 (#391) | **GL-051** | staff realtime + polling for logged-out visitors |
| GL-048 | **GL-052** | six init-time loaders querying staff tables anonymously |

Worth keeping because of how it was found: not by review, but by parsing the
register to answer "what is still open" and noticing the arithmetic did not add
up. A register nobody parses is a register that quietly rots.
