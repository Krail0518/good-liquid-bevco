| **Fixed** — #297, release-blocking + STILL PRESENT added || **Fixed** — #297, fires at both DST hours || **Partly fixed** — #297 deleted the fake-green jobs; reviewer still not connected || **Fixed** — #310, scoped to client_id is null || **Fixed** — #310, 59 unbacked grants revoked (not 2) || **Fixed** — #309, active-status check added || **Fixed** — #309, 15-min freshness window || **Fixed** — #309, declared from live state || **Partly fixed** — #296 strips at ingestion; template escaping still open |# Technical Debt Register

Per the engineering standard §12. Each item carries severity, affected module,
business impact, recommended fix, and status.

**Source:** a 14-agent audit of `origin/main` on 2026-08-28, run across seven
dimensions (tenant isolation, silent writes, browser storage, injection, edge
functions, test coverage, release automation). Every finding was then given to a
separate agent whose job was to *refute* it. Four were killed outright and five
had their severity corrected; only survivors are listed here.

Severities are the post-verification values. Where the original grading was
wrong, the correction is stated in the notes.

## Status key

- **Fixed** — corrected and verified
- **Applied** — migration live in production
- **Accepted** — owner has accepted the risk; no action planned
- **Open** — not yet addressed

## Where this stands — 2026-08-28 (end of day)

| | |
|---|---|
| Findings recorded | 34, plus 2 from the independent audit |
| Fixed or applied | 24 |
| Merged to `main` | all 14 PRs (#295-#311) |
| Migrations applied AND committed | 6 |
| Edge-function deploys completed | 3 |

Production and the repository now describe each other. Verified after the last
merge: the full invariant sweep passes including the authenticated-identity
probe, the three-identity pricing check holds in both directions, invoice
enumeration returns a uniform miss, and all nine plain-node suites pass on
`main`.

Two things the audit asked for were not code. Both are now done:

- The weekly review calls an independent reviewer (#324). It needs the
  `OPENAI_API_KEY` secret, which only the owner can supply; until then the job
  fails deliberately rather than reporting green.
- The `production-db` environment was created on 2026-08-29 with a required
  reviewer, so the `apply-sql.yml` gate (#298) is enforced rather than merely
  referenced. It had never existed: a workflow naming an environment does not
  create it, so the gate read as protection while providing none.

---

## Fixed / applied

| ID | Sev | Module | Issue | Business impact | Fix | Status |
|---|---|---|---|---|---|---|
| GL-001 | BLOCKER | `src/services/auth.js` | The one-shot localStorage→DB settings migration deleted the local copies without ever writing them to `app_settings`, and set a guard flag so it could never retry. A seeded empty default (`sign_templates '{}'`) also masked three real values. | First admin login after deploy would silently lose the SMS recipient and alert phone, five notification toggles, Dropbox Sign template map, Stripe publishable key and Sentry DSN. Recovery meant re-entering each by hand. | Upsert before delete; abort cleanup on failure so it retries; treat empty seeds as absent. | **Fixed** — `fix/app-settings-data-loss`, 20 checks pass, 12 fail against pre-fix code |
| GL-F1 | BLOCKER | `handle_new_user()` | Signup trigger was a denylist: any auth user without portal metadata got an active `sales` profile. A bare `POST /auth/v1/signup` with the publishable key qualified. | An active profiles row *is* `is_gl_staff()`, which cleared the tenant guard and reached the legacy `USING(true)` policies — full CRUD on every table, `client-docs`, and `link_customer_user_by_email`. Portal customers are competing brands. | Inverted to an allowlist keyed on `auth.users.invited_at`, which the admin API sets and signup cannot forge. | **Applied** — `20260828175051`. Four-branch behavioural test in a rolled-back transaction; no account had used the hole |
| GL-F3 | MEDIUM | storage policies | `compliance-photos` carried `FOR ALL TO authenticated` with only a `bucket_id` test, plus an `anon` SELECT permitting enumeration. | Would give every portal customer full CRUD on FDA evidence — hold tags, defect photos, PCQI-signed documents. | Replaced with a staff-gated policy mirroring `client-docs`; dropped the anon policy. | **Applied** — `20260828180304` |
| GL-XSS | BLOCKER | `src/modules/pipeline/calendar.js`, `booking-confirm` | Day panel concatenated `ev.title` / `ev.notes` into `innerHTML`. Those originate in the public tour-booking form. | A stranger books a tour with an `<img onerror>` name; script runs in a staff session — the origin holding the staff JWT. Same shape as the quote-form incident in `CLAUDE.md`. | Panel rebuilt with `createElement`/`textContent`; angle brackets stripped at ingestion across all 12 downstream uses. | **Fixed** — `security/calendar-stored-xss`. ⚠️ Test has not executed locally; CI is its first run |

**Correction on GL-F3:** first reported as live exposure. It was not — the
`compliance-photos` bucket does not exist (`storage.buckets` holds only
`client-docs`), so both policies were dangling with zero reachable objects.
It was latent, and downgraded accordingly. Hardened before the bucket exists so
it is safe by default.

## Accepted

| ID | Sev | Module | Issue | Business impact | Recommended fix | Status |
|---|---|---|---|---|---|---|
| GL-F2 | — | `profiles` | One account holds both an active staff profile and a `customer_users` portal link, created inside the window when the signup trigger wrongly gave every auth user a staff profile. | None. Owner confirmed 2026-08-28 that the account is his own, used for portal testing — not a customer. | None. Re-check if the account is ever handed to a real client. | **Accepted** |

## Open — security

| ID | Sev | Module | Issue | Business impact | Recommended fix | Status |
|---|---|---|---|---|---|---|
| GL-002 | HIGH | `index.html` | `saveRole` and `deactivateUser` call `renderPermissionsPanel()`, which is IIFE-local in `src/services/permissions-service.js` and never exported. The `ReferenceError` throws *before* the `UPDATE`. | Role changes and deactivations never reach the database. A deactivated staff member can still sign in. Presents as a dead button, not a false success. | Move the render call after the write, or export the function; then adopt the `src/services/auth.js:408` pattern — `.select()`, and treat a 0-row result as failure. | **Fixed** — #300 |
| GL-003 | HIGH | `stripe-checkout-session` | No caller authorization; looks up invoices with the service-role key by guessable sequential invoice number. | Anyone reaching the function can walk the invoice-number space and read every client's amount and payment status. | Require the invoice's `share_token` alongside the number. | **Fixed** — #307 |
| GL-004 | HIGH | `qbo-callback` | The OAuth `state` is read but never validated, and both QBO endpoints are unauthenticated. | The QuickBooks connection can be silently replaced by a stranger. | Validate `state` against a stored nonce; require staff auth. | **Fixed** — #308 |
| GL-005 | HIGH | `.github/workflows/apply-sql.yml` | Can run SQL against the production database from any branch; the path guard never checks the file was merged. | An unreviewed migration can reach production, bypassing the review gate entirely. | Restrict to `main`, require an environment approval. | **Fixed** — #298 |
| GL-006 | HIGH | `booking-confirm` | Interpolated anonymous-caller values into the approval email HTML. #296 added an ingestion strip, but it never covered `booker_email` — that field was only shape-validated by `/^[^@s]+@[^@s.]+.[^@s]+$/`, which excludes just `@` and whitespace. Neither is needed for a payload: `a<svg/onload=alert(1)>@b.co` passes it. | The payload lands in the email carrying the Approve link a staff member clicks. | Escape at the template (`escapeHtml` applied to every value the function did not construct itself), plus an explicit reject of `<>"'` backtick in the address. `stripAngles` stays as defence in depth. | **Fixed** — #318 |
| GL-007 | MEDIUM | `stripe-webhook` | Requires `verify_jwt=false`, but neither deploy path configures it. | A routine redeploy silently breaks payment reconciliation. | Set it in `config.toml`. | **Fixed** — #309, declared from live state |
| GL-008 | MEDIUM | `mailgun-webhook` | Signatures are verified but the timestamp is never checked. | Captured callbacks are replayable indefinitely. | Reject timestamps outside a short window. | **Fixed** — #309, 15-min freshness window |
| GL-009 | MEDIUM | `delete-staff-user` | Hand-rolled caller check omits the inactive-account guard. | A deactivated super-user can still hard-delete auth accounts. | Use the shared `requireStaff()` helper. | **Fixed** — #309, active-status check added |
| GL-010 | MEDIUM | migrations | `capacity`, `case_studies`, `resources` still carry the 2026-05-18 `USING(true)` policy and sit outside the tenant guard. | Any authenticated user can edit or delete published marketing content. | Drop the three `authed all` policies, or add the tables to the guard. | **Applied** — 20260828221639, #306 |
| GL-011 | MEDIUM | migrations | `anon` holds `SELECT` grants on `formulas` and `yield_logs` with no policy justifying them. | Inert today, but the formula vault sits one stray policy from public — the exact shape of the 2026-05-18 incident. | `revoke select on public.formulas, public.yield_logs from anon;` | **Fixed** — #310, 59 unbacked grants revoked (not 2) |
| GL-012 | MEDIUM | `gl_tour_intake_submit` | The `UPDATE` is scoped by email alone and does not exclude rows carrying a `client_id`. | An anonymous caller who knows a client's contact email can overwrite that client's stored formula/ingredient answers. | Add `and client_id is null` to the `WHERE`. | **Fixed** — #310, scoped to client_id is null |
| GL-013 | LOW | migrations | `20260721000000_tour_alerts.sql` passed the literal `'gl-notify-2026-abc123'` to `vault.create_secret`. That value authenticated the database's own triggers to the `notify-deal` edge function, and it was committed — anyone with repo read access held the credential. | It is in git history permanently and cannot be un-leaked, only rotated. Production was rotated on 2026-07-30 by `20260730002000` and the live value was confirmed not to equal the literal, so the exposure is closed. The file still mattered: migrations get replayed, and a fresh environment would re-create the published credential. | Migration generates the value in-database and raises if the published literal is ever live. Two guards at different layers: `tests/committed-secrets.test.cjs` keeps literals out of migrations, and `SECRET` facts in the drift snapshot report whether production still accepts a known-published value — recording only a length and a boolean, never a value. | **Fixed** — #316 |

## Open — data integrity

| ID | Sev | Module | Issue | Business impact | Recommended fix | Status |
|---|---|---|---|---|---|---|
| GL-020 | HIGH | invoicing | Every "mark invoice paid" path writes AR status without `.select()` or a rows-affected check; one has no error handling at all. | RLS rejects silently — the UI reports success while nothing saved. `CLAUDE.md` attributes ~40 bugs to this class. | Append `.select()`; treat `error` **and** an empty array as failure. | **Fixed** — #301 |
| GL-021 | HIGH | `index.html` | Six delete paths check only `.error`, never rows-affected. | Deletes report success, purge the local cache, and write a false audit entry. | Same pattern as GL-020. | **Fixed** — #302 |
| GL-022 | HIGH | pipeline / clients | Deal, referral, referrer and client creation fall back to a browser-only synthetic id when the INSERT fails. | Business records exist only in that tab and vanish on reload. | Fail loudly instead of synthesising an id. | **Fixed** — #303 |
| GL-023 | HIGH | compliance | "Reset CCP limits to defaults" clears only the local cache; the DB override still wins. | FDA critical limits are never actually reset. | Delete the DB override. | **Fixed** — #305 |
| GL-024 | HIGH | admin tools | "Clear local cache" offers pre-checked deletion of legacy blobs that are still the only copy, labelled "safe to wipe". | Silent business-data loss initiated from a UI that says it is safe. | Migrate the blobs, or remove them from the default selection. | **Fixed** — #305 |
| GL-025 | ~~MEDIUM~~ | `src/shared/tools.js` | ~~Service package pricing lives only in per-device localStorage.~~ **Withdrawn — not a defect.** The finding was read off the stale `C:\Users\mike\Desktop\goodliquid` snapshot, which still has `gl_service_packages` in localStorage. This repo moved the catalog to `app_settings.service_packages` in `921eef0`, and `src/shared/tools.js` has used `glGetSetting`/`glSaveAppSetting` ever since. | None. | None needed. Verified against the real repo on 2026-08-29. | **Withdrawn** — audit artifact |
| GL-026 | MEDIUM | CIP audit | A rejected DB write leaves an FDA-required CIP record in `gl_cache_compliance_records`, but the CIP page read `gl_cip_logs` — a key nothing has ever written, because its only writer `saveLocal()` was never called. The fallback was therefore unreachable, and the page rendered "No cycles logged yet". | The operator cannot tell a sanitation record that failed to file from a cycle nobody logged. | Read the key the writer actually writes; show unsaved cycles with a retry that checks rows-affected. | **Fixed** — #314 |
| GL-027 | MEDIUM | compliance | `public.cip_logs` exists with RLS but holds 0 rows; the 72 real CIP records live in `compliance_records` under `GMP-SAN-002`. An error message pointed debuggers at the empty table, and `auditor.html` made three fetches whose results were never read. | Wasted debugging time; ambiguity about the system of record; three dead round trips per auditor page load. | ADR-0001 names `compliance_records` the system of record; dead readers removed. Dropping the table itself is separate — it changes the authorization surface. | **Fixed** — #314 |

## Open — architecture and process

| ID | Sev | Module | Issue | Business impact | Recommended fix | Status |
|---|---|---|---|---|---|---|
| GL-030 | HIGH | `.github/workflows` | The weekly AI review was an echo-only placeholder; `ci.yml` and `security-scan.yml` were unconditionally-green jobs running under trustworthy names. | The review gate the standard describes did not exist in executable form, while appearing to. | #297 removed the fake-green jobs. #324 connects the reviewer: `scripts/ai-review.mjs` calls an **independent** model (OpenAI, not Anthropic — `AGENTS.md` splits the roles so the reviewer did not write the code), and `scripts/ai-review-file-issues.mjs` files each finding as an issue carrying all six fields Setup Instruction #1 requires, commenting on the existing issue rather than duplicating a recurring one. Every "did not actually review" path — missing key, HTTP error, unparseable output, no findings array — **fails** rather than reporting zero findings. | **Fixed** — #324, #331; needs `OPENAI_API_KEY` (preferred) or `ANTHROPIC_API_KEY` (accepted, same family as the implementer, and every run and issue says so) |
| GL-031 | HIGH | deployment | Nothing prevents production deploy when the gate fails — Vercel auto-deploys on push to `main`. | A failing review cannot stop a release. | Branch protection + required status checks. | **Fixed** — #313; `main` requires `smoke`/`invariants`/`drift`, strict. `enforce_admins` off and no required reviews, both deliberate and reasoned in `docs/runbooks/deployment.md` |
| GL-032 | HIGH | `scripts/security-invariants.sh` | Only ever authenticates as `anon`; never tests the `authenticated` role. | Cannot catch the 2026-05-18 class of hole it was written for. `CLAUDE.md` asks for all three identities. | Add a section signing in as a throwaway self-registered account, asserting zero rows from `clients`, `invoices`, `formulas`. | **Fixed** — #299 |
| GL-033 | HIGH | `tests/full-sweep.cjs` | Scores a missing global as PASS. | A module that stops loading turns 18 checks green. | Fail on absence. | **Fixed** — #304 |
| GL-034 | MEDIUM | tests | The `pageerror` noise filter swallows genuine app crashes — `Maximum call stack` was listed as expected noise, and substring matches on `supabase`/`status of 4` discarded null-derefs that merely named a library. | Guts the "no fatal JS error" assertion in every browser test. | `ALWAYS_APP_BUG` checked before the noise list, in both suites; `test-integrity` drives the real regexes. | **Fixed** — #313 |
| GL-035 | MEDIUM | `.github/workflows` | The Tuesday deep-review cron `0 12 * * 2` is 08:00 EDT but 07:00 EST; no timezone configured. | Drifts an hour every winter. Setup Instruction #1 explicitly requires an intended timezone. | Run at `0 11,12 * * 2` and exit unless the local New York hour is 8. | **Fixed** — #297, fires at both DST hours |
| GL-036 | MEDIUM | `.github` | The AI-review issue template omits the release-blocking field and the STILL PRESENT re-review state; the PR template omits 7 of the 15 §15 checklist items. | The remediation loop has no field to gate on. | Add the missing fields. | **Fixed** — #297, release-blocking + STILL PRESENT added |
| GL-037 | MEDIUM | architecture | 305 top-level functions in one ~9,300-line inline script inside `index.html`, plus 55 `crm-*.js` files at the repo root. | §3 names this directly. Highest-blast-radius area for unintended effects. | **Done.** #321 lifted the inline block into `crm-index-core.js`; #325–#329 extracted 32 capabilities from it; #341–#348 relocated the remaining 55 root files in seven risk-ordered groups. **86 modules** now under `src/modules/<domain>/`, `src/services/` and `src/shared/`; `crm-index-core.js` is the only `crm-*.js` left at the root, as the entry point. Every move byte-identical with script order compared index-by-index. Guards pin the taxonomy, load order, the classic-script contract, that every module on disk is loaded, and that no second root module appears. The migration also surfaced three live defects the tests could not previously see: an unchecked meeting-note delete and a blind delete/insert pair on `user_permissions` (both GL-021 class). | **Fixed** — #321, #325–#329, #341–#348 |
| GL-038 | LOW | product | The `compliance-photos` bucket had never been created, so hold-tag photos, defect photos and PCQI document signing all failed. The UI told staff to create it in the Supabase dashboard — the change CLAUDE.md rule 2 forbids. | A documented compliance feature did not work. | Created **private** by migration, matching `client-docs` and `sales-decks`; a public bucket would serve FDA evidence to anyone holding a guessable URL. Client moved off `getPublicUrl()` to storing the path and signing on display. | **Fixed** — #315 |
| GL-039 | MEDIUM | tests | Four test files existed in `tests/` but were referenced by no workflow, so they never ran: `test-integrity`, `cip-system-of-record`, `formulation`, `training-edit`. One of them was the guard written for GL-034. | Same failure mode as GL-034 — the suite credits itself for coverage it is not providing, and the file passing by hand hides it. | All four wired; `test-integrity` now fails if any test file is referenced by no workflow. Wiring `formulation.test.cjs` immediately caught GL-041, a live data-integrity bug it had been silently guarding against all along. | **Fixed** — #314 |
| GL-040 | LOW | client-wide | 19 `localStorage.getItem` keys had no literal `setItem`. **Triaged: no defects.** 13 are one-way legacy migration readers guarded by a `_migrated` flag; `gl_mailgun_key` and `gl_customer_logins` are read only in order to `removeItem` them (deliberate cleanup of legacy secrets); `gl_sms_to_phone` is an alias fallback whose primary key `gl_sms_alert_phone` **is** written; `gl_active_timer` is a legacy reader explicitly listed as transient. `gl_mailgun_from` is dead config where the hard-coded default always wins — harmless. | None. The scan counts an upper bound, not defects: it cannot see writes through variable keys, and a one-way legacy reader is correct by design. | Closed by triage. The one genuine problem it surfaced is tracked separately as GL-043. | **Closed — no defect** |
| GL-043 | HIGH | `src/modules/production/compliance-ext.js` | `getCurrentUserId()` read `window.__GL.session.user`. **Nothing in this codebase ever assigns `window.__GL`** — it is read in two places and written in none — so the function returned `null` on every call. Every other user lookup in the same file already used `window.currentUser`. | The null fed `inspector_tokens.created_by` (who granted an external FDA auditor access to compliance data) and `compliance_records.second_signed_by` (the dual-PCQI co-signature, whose only other field is free text typed at a `prompt()`). A co-signed FDA record was attributable to nobody. Latent: 0 co-signed records and the one existing token came via another path. | Read `window.currentUser` first, keep `__GL` as a fallback; refuse the co-signature outright when the signer cannot be identified. Deleted the dead `getCurrentUserName()`, which read the same never-set property and fell back to a hard-coded person's name. | **Fixed** — #319 || GL-041 | HIGH | `src/modules/pipeline/formulation.js` | `glLoadFormulators()` set `CACHE = CACHE || []` both when Supabase was not ready and when the fetch threw. `[]` is truthy, so the `if(CACHE && !force)` guard short-circuited on every later call and the house list stayed empty for the rest of the session. One early call — the warm-up poll, or any form binding before the client is up — was enough to poison it. | The formulator dropdown never seeds, ticking "formulation done" cannot auto-pick a house, and the deal saves `formulation_vendor: null`. Formulation revenue is attributed to no one. Deals are affected; clients happened to escape only because adding a formulator forces a refresh first. | Only cache a load that reached the server; leave `CACHE` null so the next call retries. | **Fixed** — #314 |
| GL-044 | HIGH | `index.html` | `logoutCRM()` and `saveReferrer()` were each declared twice at top level in the same inline script. Two `function` declarations of one name do not conflict — the **last silently wins for every call site**, including the Sign out button, which appears textually above both. For `logoutCRM` the winner was the correct one (calls `supa.auth.signOut()`, resets `crmInited`); the dead copy cleared local state only. | Sign-out ended the Supabase session **by accident of declaration order**. GL-037 extracts this block one capability at a time, which reorders code by definition — moving the live copy above the dead one, or extracting one and not the other, would have silently reverted logout to "clears the screen, keeps the session", with nothing failing and nothing visible in review. | Both dead copies removed. `tests/duplicate-declarations.test.cjs` fails on any repeated top-level name, and asserts the surviving `logoutCRM` still signs out. Treat as a precondition for GL-037. | **Fixed** — #320 |
| GL-042 | HIGH | `index.html`, `documents` | The Documents page had never worked, for three stacked reasons. `saveDocument()` inserted `file_url` and `file_path`; **neither column existed** on `public.documents`, so PostgREST rejected every insert with PGRST204. The value being stored was a `getPublicUrl()` on the **private** `client-docs` bucket, which returns HTTP 400. And `renderDocs()` drew a delete button but **no download control at all**, so even a saved document was write-only. | Production held 0 rows. Each attempt uploaded the file first, leaving it in the bucket with nothing pointing at it. | Added `documents.file_path` by migration (with a guard refusing a `file_url` column); the uploader returns a path; the page signs on demand and offers a Download. The insert now uses `.select('id')` and treats 0 rows as failure. | **Fixed** — #317 |
| GL-DEF-01 | MEDIUM | `vercel.json` | The CSP allows `'unsafe-inline'` in `script-src`, because **550** inline `on*` handlers across 48 files resolve against `window`. | Weakens the main defence-in-depth control against injected script. The escaping rules in CLAUDE.md rule 5 are what actually stand between a lead-supplied string and a staff session. | **Phases 1–6a done — #350–#356.** `src/shared/actions.js` is a delegated dispatcher resolving `data-gl-action` against an **allowlist registry**, not `window[name]`, so a converted control reaches only what was registered. Arguments travel as `data-gl-arg1="${esc(v)}"` read via `dataset` — not JSON-in-attribute, which rule 5 forbids; proven with 10 hostile payloads in a real browser before the file was written. Phase 2 converted the 52 pure-close handlers to `data-gl-close`, verified by clicking all nine close paths in a real browser. One was refused: `this.closest(div[style*=fixed])` needs `closest` semantics rather than `querySelector`, and extending the dispatcher for a single site is worse than leaving it. `tests/inline-handler-budget.test.cjs` is the ratchet — a per-file budget that fails if a count goes UP or if a budget is left with slack. Phases 2–7 in the plan: close buttons + hover CSS (65), `index.html` (234), core (73), 45 modules, the 145 needing extraction, then drop `unsafe-inline`. | **Fixed** — #355, #357, #386. 550 → 0 handlers; `script-src` no longer carries `'unsafe-inline'`, verified enforcing in production. This row sat at "phase 6a of 7" long after the work finished. |
| GL-045 | INFORMATIONAL | `public.capacity` | The one remaining anon policy with `USING (true)` — the shape CLAUDE.md rule 1 forbids. Found during the end-of-work verification sweep, not by a test. | **Not a leak.** The table holds `quarter`, `booked`, `cans_capacity`, `bottles_capacity`, `week_start` — no client identifier — and the public marketing site reads it. Every other anon policy is scoped (inspector-token gated, `published = true`, `is_active = true`, or insert-only public forms). | Left permissive on purpose; scoping it would break the public site for no gain. But its safety is an assumption about the schema, and CLAUDE.md's central lesson is that assumptions expire silently — so section 2b of `security-invariants.sh` now probes it from outside with the anon key and **fails if the table ever gains a client-identifying column**. | **Closed — enforced assumption** |
| GL-046 | HIGH | `supabase/functions/*` | **18 edge functions import `https://esm.sh/@supabase/supabase-js@2` — a floating major.** esm.sh resolves it to whatever the newest 2.x build is at request time (2.112.4 today). The code that runs tomorrow is not the code that was reviewed today, and nothing in the repo changes when it moves. | **All 18 hold `SUPABASE_SERVICE_ROLE_KEY`**, which bypasses RLS completely. Whatever that URL serves runs with unrestricted read/write on every client's data — the outcome CLAUDE.md names as the worst this system can produce. No off-the-shelf scanner would ever report it: there is no package.json, so `npm audit` has nothing to read and Dependabot sees only the workflow files. | Guard added first: `tests/supply-chain-pinning.test.cjs` froze the count so it could not grow. All 18 then pinned to `@2.112.4` — **the exact build esm.sh already served for `@2`**, verified by `X-Esm-Path` resolving to a byte-identical path, so the change is behaviour-preserving by construction. The test baseline is now **zero**: any reintroduced floating specifier fails CI. Deployed with `gh workflow run "Deploy Supabase"`, since edge functions do not ship on merge. | **Fixed** — #364 |
| GL-047 | MEDIUM | `index.html:3228` | The Chart.js `<script>` tag pulls from `cdn.jsdelivr.net` with an exact version but **no Subresource Integrity attribute**, and the CSP `script-src` allows that origin. | A CDN compromise or a swapped file at that URL executes arbitrary script in staff sessions — the same reach as the stored-XSS class CLAUDE.md rule 5 exists to prevent, just entering through a different door. Version-pinning alone does not help: the pin names a path, not a file's contents. | Added `integrity="sha384-e6cc9LaI…" crossorigin="anonymous" referrerpolicy="no-referrer"`. The hash was taken from the file jsDelivr actually serves and confirmed **three ways**: two independent fetches, plus a vendored copy then present in `index_files/` (since deleted as dead weight) — all byte-identical (205,488 bytes). Verified in a real browser rather than assumed, because a wrong hash blocks the script site-wide: `Chart` loads as **4.4.2** and renders a chart, while an injected tag carrying a deliberately wrong hash is **blocked**, proving the browser enforces SRI here rather than ignoring the attribute. Test baseline is now zero. | **Fixed** — #366 |
| GL-051 | LOW | `src/modules/customers/requests.js`, `src/shared/help-features.js` | The public marketing site opened **two staff-only realtime channels** (`customer_requests`, `invoices`) and polled `customer_requests` every 60s for visitors who were not logged in. | Not a data leak — RLS refuses all of it, which is exactly why it stayed invisible. It was wasted work on every public page view: two WebSocket subscriptions and a poll timer running for someone reading the marketing copy, plus console noise that makes a real error harder to spot. | Both gated on `window.currentUser`. **Polled, not checked once** — these modules run at DOMContentLoaded and sign-in happens later with no page reload, so a single check would have silently killed the paid-invoice notification. That global was the open question: I could not tell by reading whether the staff login path sets `window.currentUser` or the module-scoped copy in `crm-index-core.js`, and guessing wrong breaks a working feature quietly. Mike ran a read-only console snippet on a live admin session, which showed `windowCurrentUserSet: true` with both channels joined. Both sides then verified: anonymous load goes 2 channels to **0**, and setting the global the way sign-in does brings both back within the poll interval. The banner refresh also re-checks per call, so polling stops at sign-out too. | **Fixed** — #391 |
| GL-048 | MEDIUM | CI | The repository had **44 named CI steps and zero scanners** — no secret scanning, no dependency scanning, no `dependabot.yml`. CLAUDE.md rule 6 ("secrets never live in `crm-*.js`, `index.html`, or a migration") was enforced by attention alone. | A committed secret would have been found by whoever noticed it, which is the same control that failed for the dashboard-applied anon policies. | Added `.github/workflows/security-scan.yml` (gitleaks, pinned to 8.30.1 and checksum-verified) plus `.github/dependabot.yml` for the one manifest that exists. History was scanned by hand first — 1,414 blobs, zero provider keys, one JWT with `role=anon` (public by design) — so the job did not switch on over a pile of pre-existing findings everyone would learn to ignore. The allowlist is itself guarded: an anon and a `service_role` token for this project share a byte-identical base64 prefix for 73 characters, so the obvious allowlist would have hidden the one finding that matters. | **Fixed** — #359 |
| EXT-002 | HIGH | `src/services/auth.js` | A null profile lookup fabricated `{role:'sales', status:'active'}` and logged the user in. `_glFetchProfile` returned null on a **missing row, a failed query, OR a thrown error** — all three landed there. Supabase signup is open, so a self-registered stranger was handed an active salesperson identity and shown the CRM shell. | The database was never fooled — RLS refuses those queries, which is exactly why the production invariant probe kept passing and nothing looked wrong. But that is the second line of defence doing the first line's job, and it contradicted `20260828175051_staff_profile_requires_invite.sql`, which had just made the same rule fail closed in the database. | Fails closed: no row, no role, or a non-active status denies the login and signs the session out. A **deny**-list not an allow-list — an allow-list of the roles I could find would lock out any role I had not seen, and `customer_users` is constrained to exactly ('owner','member') so the portal roles are knowable. `tests/auth-fail-closed.test.cjs` covers six denial paths plus the success path, so the guard cannot be satisfied by simply breaking login. | **Fixed** — #378 |
| EXT-028 | HIGH | `sw.js` | The service worker cached **every** successful GET with no origin, status or type check — including cross-origin Supabase REST responses holding one client's invoices, formulas and documents. | Those responses sat in the browser Cache API, survived logout, and would be served to whoever used that browser profile next. On a shared machine that is cross-tenant disclosure — the outcome CLAUDE.md names as the worst this system can produce. None of the 39 test files touched the service worker. | Cross-origin requests are no longer intercepted at all (no `respondWith`, so no code path can cache them). Two things were **not** obvious and were found only by testing: renaming the cache does **not** reliably evict what the old worker stored, and the browser kept running the previous `sw.js` from its HTTP cache so the new purge never ran — `updateViaCache:'none'` was what actually fixed it. | **Fixed** — #377 |
| EXT-005 | MEDIUM | `supabase/functions/{ai-proxy,mailgun-send,send-sms,dropbox-sign}` | Four endpoints that call paid vendors had authorization but no rate limit. | Rule 7 was already satisfied — all four require a staff session, so a stranger cannot reach them. But authorization decides *who* may call and says nothing about *how often*, and these are the four endpoints where "how often" has an invoice attached: Anthropic tokens, Mailgun sends, Twilio messages, Dropbox Sign envelopes. A stolen staff session could loop any of them. | New `public.edge_rate_limits` + `gl_rate_limit_hit()`, fixed windows, service_role only, RLS with no policy. **Fails open by design**: a limiter that takes email and AI down when a table blips does more damage than the abuse it prevents, and the abuse case needs a compromised account first. | **Fixed** — #384 |
| EXT-025 | MEDIUM | 8 modules | Eight `.update()` calls checked `r.error` and stopped there, then updated local state or told the user it worked. RLS refuses silently — zero rows, no error. | Worst was `portal-public.js:212`: on a silent refusal it still returned a link built from a share token the database never stored, so the customer received a URL that could not resolve and nothing reported a problem. | `.select()` plus a row-count check on all eight. **Triaged rather than swept** — 26 unchecked calls exist, but 18 assert no success at all (fire-and-forget AI brief state, telemetry), and bolting alerts onto a background touch is noise pretending to be rigour. One stays by choice: `compliance-ext.js:669`, whose `return true` means "token valid", not "write landed". | **Fixed** — #383 |
| EXT-012 | MEDIUM | `supabase/functions/booking-confirm`, `public.bookings` | The conflict check and the insert were separate statements with nothing between them, so two public requests for the same slot could both pass the check. | Two ordinary visitors clicking the same slot in the same second is not adversarial, it is Tuesday. Note the audit was **wrong** that throttling was absent — 3/email/day and 20/hour exist; the limit is written as plain counts rather than anything named like one, and I nearly repeated the error. | An **exclusion** constraint, not a unique key: slots are ranges and the failure is overlap, so 2:00–2:30 and 2:15–2:45 collide on no single column. The predicate covers only statuses that occupy a slot — `declined` is in live use and must stay free to overlap. `booking-confirm` turns 23P01 into a 409 with "that time was just booked" rather than a 500. | **Fixed** — #382 |
| EXT-030 | LOW | `supabase/functions/booking-availability` | Public, unauthenticated, called Google Calendar on every request, and its date was format-checked but never range-checked — `9999-12-31` was a valid query. | Anyone could walk dates indefinitely and spend Mike's Calendar API quota, the one resource here metered by somebody else. Confirmed the endpoint returns only `{time, available}` — **the audit's claim that it exposes personally identifiable calendar data is false**. | Dates outside a real booking window (yesterday to +365 days) are answered from the request and never reach Google. Deliberately not a rate limiter: it removes the incentive rather than counting requests, and needs no state. | **Fixed** — #382 |
| EXT-024 | LOW | `crm-index-core.js`, `invoice-builder.js` | Invoice numbers were computed in the browser by two different generators — `max(loaded)+1`, and `'GL-' + year + '-' + Math.floor(Math.random()*9000)` in a format no stored row uses. | **Smaller than first reported, and the correction matters.** I claimed a duplicate could make Stripe charge the wrong invoice. `invoices_invoice_number_key UNIQUE` **already exists in production** — created by no file in `supabase/migrations/`, which is why grepping the directory missed it. Duplicates were never possible; a collision produced a failed save, not a bad charge. | `gl_next_invoice_number()` allocates under `pg_advisory_xact_lock`, staff-gated. Called at SAVE time, not render — `#ginv-id` is filled when the builder opens and can be minutes stale. Falls back to the local computation if the RPC fails, with the constraint as backstop: losing the ability to invoice would be worse than a rare retry. | **Fixed** — #380 |
| EXT-032 | MEDIUM | `billing-admin.js`, `password-change.js` | JSZip and jsPDF were injected with `document.createElement('script')` from a CDN with **no integrity attribute**. | A script injected from JS is exactly as privileged as one in the markup. **My own supply-chain guard could not see them** — it scanned static `<script src>` tags and reported "0 without SRI" the entire time, while the Chart.js tag beside them had one. | SRI added, hashes taken from the served files and confirmed stable. The guard now walks `.src` assignments too, matching on the **variable** rather than proximity — a line-window version produced a false positive on my own fix, because a comment pushed `.integrity` seven lines down. | **Fixed** — #379 |
| EXT-029 | MEDIUM | `portal-customer.js`, `password-change.js` | Temporary passwords built from `Math.random()`, which is not a cryptographic RNG. | One of the three is **emailed** as a reset password, so a predictable value there is directly usable by anyone who knows roughly when a reset happened. | `crypto.getRandomValues` with rejection sampling — 256 is not a multiple of the 57-character alphabet, so a bare modulo would bias the first characters. The `GL!`/`aZ1` affixes are kept deliberately: they guarantee the complexity rule the auth provider applies. | **Fixed** — #376 |
| EXT-031 | LOW | `compliance.js`, `crm-extras.js` | Neither CSV escaper neutralised formula prefixes. A cell beginning `=`, `+`, `-` or `@` is read by Excel as a **formula**, and these exports carry lead, customer and staff free-text. | Quoting does not help — the quotes are consumed by the CSV parser and the formula survives, which is why both escapers looked correct and were not. | A leading `-` is only neutralised when what follows is **not a number**: invoice amounts and credits are legitimately negative, and prefixing `-25.00` would turn every negative figure into text and break the sums the export exists for. Both copies fixed — fixing one would have left the other exporting the same data unguarded. | **Fixed** — #381 |
| EXT-035 | LOW | `SECURITY.md` | The document still showed `script-src` with `'unsafe-inline'`, explained why it was "required", and listed its removal as future work. The secret table pointed at `fix.js` for a key that is not there and names the wrong key type. | A security document that overstates enforcement is what an auditor, a customer or a future maintainer reasons from. The audit that prompted this work made exactly that mistake at a larger scale; leaving our own doc stale would be the same error committed deliberately. | Corrected to describe what is actually enforced, what it took (134 handlers, 6 inline blocks, 29 `javascript:` URLs), which guards keep it true, and that `style-src` still carries the allowance and why that is separate. | **Fixed** — #385 |
| EXT-036 | MEDIUM | `compliance.js`, `compliance-ext.js` | Five Print/Close buttons in the compliance report popups were dead. The views are written into `window.open('','_blank')`, and the dispatcher listens on the **main** document — the popup has no listener at all. | **A regression I introduced.** GL-DEF-01 phase 5 (#355) converted `onclick="window.print()"` along with everything else; converting markup rendered into a *different document* was wrong. It failed silently because the dispatcher that would log "no action registered" is not in the popup either — and `close` hid it further, since `glRegisterGlobalActions` resolves names against `window` and **`window.close` is a real method**, so the registry check could never flag it. | Restoring the inline handlers is impossible — a `window.open('')` document inherits the opener's CSP, which no longer permits inline script. The opener binds them instead, so no script runs in the popup. `inline-handler-budget.test.cjs` now fails if a file writes `data-gl-action` into a popup without binding it, scoped to the region between `window.open()` and the matching write after a first version produced three false positives. | **Fixed** — #386 |
| GL-DEF-02 | MEDIUM | `vercel.json`, 6 pages, 16 modules | `style-src` allowed `'unsafe-inline'`, which covers both style **elements** and style **attributes**. 28 style elements existed: 7 inline `<style>` blocks in the pages, 10 built at runtime with `document.createElement('style')`, and 11 generated into print/report popups. | The two halves are not equally dangerous, and that is the whole reason to separate them. A `<style>` ELEMENT can carry attribute selectors — `input[value^="a"]{background:url(...)}` — which is the primitive behind CSS keylogging and CSS exfiltration. A style ATTRIBUTE applies only to its own element and cannot select anything. Leaving both under one permissive directive meant the dangerous half was open in order to keep the harmless one working. | All 28 moved to stylesheets, then the directive split: `style-src-elem 'self'` strict, `style-src-attr 'unsafe-inline'` unavoidable (~6,000 attributes, and no nonce can ever cover a style attribute), plain `style-src` left permissive as the pre-CSP3 fallback for Firefox <122. CSS was **evaluated** out of the source rather than retyped, since much of it is multi-line concatenation. Two needed hand work: the invoice PDF interpolated `inv.status` into the badge colours (now modifier classes) and the public invoice view is not a popup at all. One race fixed: that popup printed on a bare 350ms timeout, harmless when the CSS was inline, a coin-flip once it became a `<link>`. Verified by serving the real CSP locally — an inline `<style>` stops applying, a linked one applies, a `style=` attribute still applies. | **Fixed** — #389 |
| GL-050 | LOW | `src/shared/help-features.js`, `crm-index-core.js`, `crm-extras.js` | Three fire-and-forget calls to promise-returning Supabase APIs — `removeChannel()` and two `signOut()` — were wrapped in `try/catch`, which cannot catch a **rejected promise**. supabase-js 2.106.2 rejects out of its channel-leave path with `RangeError: Maximum call stack size exceeded`. | Measured on production, not inferred: a **returning** visitor (service worker registered, cache warm) reliably got ~18 unhandled rejections during page init. A cold browser got none, which is why it had gone unnoticed. `fix.js` pipes every `unhandledrejection` to `Sentry.captureException`, so for logged-in staff this is quota spent on noise that also buries real errors. Reproduced deliberately by calling `removeAllChannels()` on a joined channel. | The promise is handled where it was previously dropped. Deliberately silent and still not awaited — a failed channel-leave is not actionable (the channel is rebuilt on the next line) and logout must not depend on a network round-trip. I first assumed this was an unbounded retry loop and **it is not**: the channels reach `joined` and the errors stop, which the fix note records so nobody re-derives it. | **Fixed** — #390 |
| GL-052 | LOW | `compliance`, `facilities`, `formulators`, `lead-followups`, `invoices` loaders | Staff-only tables were queried at init for logged-out visitors. Measured by patching `fetch` before any page script ran and capturing a stack per request, rather than grepping: **nine** loaders, not the six the 401 count suggested — several tables answer 200 with `[]` under RLS and so never showed as 401s. One was a `setInterval`, polling `compliance_records` forever in the background of the public marketing site. | No exposure — RLS refuses every one, which is exactly why it stayed invisible. It is waste on every public page view plus console noise that buries real errors. | New `window.glWhenStaff(fn)` in `crm-index-core.js` (loaded 3rd, before every module) defers a callback until `window.currentUser` exists, polling rather than checking once — a one-shot check is the trap that nearly shipped in GL-047, because sign-in happens later without a page reload and would silently disable the feature for everyone. Applied to nine init sites; five render functions also guard internally, since a MutationObserver re-triggered them independently of boot. **Logged out: 6 → 0 401s, and only 3 queries remain, all genuinely public** (`capacity`, `case_studies`, `resources`). **After sign-in: 21 tables reached**, so nothing was disabled. `training-edit.test.cjs` needed a staff session added — it had been rendering staff UI with no session, relying on the behaviour being fixed here. | **Fixed** — #394 |
| GL-049 | MEDIUM | `public.invoices` (and likely its siblings) | `updated_at` was not maintained on **21 of the 42 tables that have the column** — including `invoices`, `clients`, `deals`, `quotes`, `referrals` and `tasks`. There was no trigger, and most write paths never set it, so the column only moved when application code happened to set it by hand. | A column named `updated_at` that does not track updates is worse than no column, because every reader assumes it does. Delta syncs, "recently changed" views and any audit question about when an invoice was altered all get a confident wrong answer. Observed live: GL-1024 went `pending` → `paid` with its timestamp frozen two hours back, GL-1015 changed status while still reading 13 July. | Attached the **existing** `public.set_updated_at()` to the 21 unwired tables — no new function, closing a gap in a convention the schema already had (20 tables were already wired). Idempotent, skips anything already maintained. Reproduced before (`timestamp_moved: false`), verified after (`true`), every probe inside a transaction that rolls back so nothing was written and realtime emitted nothing. All three identities checked: staff writes still work and now stamp; a portal customer still sees exactly 1 client and 0 staff profiles; a self-registered stranger still sees nothing. Coverage went 21/42 → **42/42**. | **Fixed** — #393, migration `20260830210000_maintain_updated_at` |
## ID collision, 2026-08-30 — read this before citing GL-046/047/048

Three rows were logged under IDs that were already taken. The next free number
was picked from a stale read of this file instead of from the file as it stood,
and GL-046, GL-047 and GL-048 had all been used earlier the same week.

Two produced duplicate rows. GL-047 was worse: the edit matched the existing row
and REPLACED it, so **the Chart.js SRI entry (#366) was destroyed rather than
duplicated** and would have vanished from the register entirely. It has been
recovered from git and is back in place.

The three new rows are renumbered. The pull requests are already merged and
their commit messages still carry the old IDs, so the mapping is:

| commit says | actually | subject |
|---|---|---|
| GL-046 (#390) | **GL-050** | three dropped Supabase promises |
| GL-047 (#391) | **GL-051** | staff realtime + polling for logged-out visitors |
| GL-048 | **GL-052** | six init-time loaders querying staff tables anonymously |
| GL-053 | MEDIUM | `crm-index-core.js` — `effectiveInvoiceStatus` | Overdue was computed, wrongly, in three different ways over one afternoon. **Original:** `new Date(dueDate) < new Date()` — a date-only string parses as UTC midnight against a moment, so an invoice due **tomorrow** flipped at 8pm tonight. **Then** `due < today`, which reported a genuinely late invoice as pending. Underneath both: there was no stated rule, and the invoice row disagreed with itself — the badge showed the stored status while the buttons used the computed one. | Whether a customer is chased is a business decision, and it was buried in an expression nobody could see or change. | Overdue is now a **policy, not a guess**: an unpaid invoice is flagged only once it is more than `invoice_overdue_grace_days` past due (**21** — three weeks — by default) and only while `invoice_overdue_enabled` is on. Both live in `app_settings`, edited from the Invoices header, read by BOTH the browser and the nightly `mark-overdue-invoices` job so the two can never drift. A status already set to overdue is honoured rather than recomputed — the grace period governs the automatic flip, not a decision somebody already made. Both invoice badges now use the effective status, so a row no longer contradicts itself. Eleven assertions pin the boundaries at 21/22 days, the switch, a configurable grace, and nonsense inputs. | **Fixed** — #395, corrected in #396 |
| GL-054 | MEDIUM | `crm-index-core.js` — invoice actions | There was **no way to undo marking an invoice paid**. `quickPaid` had no inverse, and a paid row's only remaining actions were view and delete, so correcting a mis-click meant deleting and re-creating the invoice or editing the database by hand. | Reported by Mike after a test invoice: *"once i marked it paid i did not have the option to switch it back to pending"*. A one-click irreversible action on financial records, where the only escape is deletion, invites exactly the data loss it looks like it is protecting against. | New `quickUnpaid` returns the invoice to **pending** and clears `paid_at`, `paid_amount` and `paid_method`. Deliberately does NOT choose `overdue` — that is `effectiveInvoiceStatus`'s and the nightly job's decision, and guessing it here is how an invoice ended up marked overdue on the day it was issued. Same checked-write discipline as `quickPaid`: local state changes only after the server confirms rows changed, since RLS rejects silently. Verified in a browser end to end — the button appears only on paid rows and sends `{status:pending, paid_at:null, paid_amount:null, paid_method:null}`. | **Fixed** — #396 |
| GL-055 | HIGH | `supabase/migrations/` | **The migration history cannot rebuild the database.** Ten of production’s 91 public tables are created by no migration — `profiles`, `clients`, `invoices`, `deals`, `activity`, `referrals`, `referrers`, `sales_decks`, `bottling_rates`, `canning_rates` — plus the functions `gl_is_viewer` and `admin_set_user_password`. Three more (`is_staff_user`, `is_admin_user`, `is_super_user`) are used by RLS policies in migrations that run **before** the one creating them. | `profiles` is the table `is_gl_staff()` reads, so every staff RLS policy rests on a table the repository never creates. Restoring from the repo alone was impossible, and nobody would have discovered that until they needed it. Same shape as the 2026-05-18 incident CLAUDE.md describes — things applied by hand that appear in no migration — except these are tables and functions, not policies. | Found by creating a staging project and replaying the history into it: it failed on the first migration with `relation "public.profiles" does not exist`. New `20260515000000_bootstrap_core_tables.sql` creates all ten tables and six functions, with DDL read out of production’s catalogs (`pg_attribute`, `pg_get_constraintdef`, `pg_get_functiondef`) rather than written from memory. Idempotent, and verified a **no-op against production**: 0 tables, 0 functions, 0 constraints missing there. Includes `invoices_invoice_number_key`, the UNIQUE constraint EXT-024 found in production and in no migration. | **Fixed** — #398 |
| GL-056 | MEDIUM | `supabase/migrations/20260519000008_user_permissions_actions.sql` | The file contained a **backslash-escaped apostrophe** inside an ordinary SQL string literal. Under `standard_conforming_strings` — on by default — that is a syntax error, so the migration could not run at all on a fresh database. | It is committed, it reads as fine, and it has never executed as written. Production holds the correct text, so it was applied by some other route. A migration that cannot run is not a record of anything. | Corrected to the SQL-standard doubled quote, producing the identical string. Found by replaying the history, not by reading it. | **Fixed** — #398 |
| GL-057 | MEDIUM | `supabase/migrations/` | Two structural problems that stop `supabase db push` replaying this history at all. Roughly **30 files share a date-only version prefix** — both `20260516000000_new_feature_tables.sql` and `20260516000001_qbo_tokens.sql` are version `20260516` — which collides on `schema_migrations_pkey`. And two pairs sort alphabetically into the **wrong dependency order**: `customer_portal` before the `phase4_sql_pack` that creates the table it references, and `permissions_audit` before `user_permissions`. | Production never hit either, because these were applied by hand rather than pushed. The consequence is that the standard tool cannot rebuild or verify the schema. | All 131 migrations renamed to unique 14-digit versions with `git mv` (history preserved), and the two dependency pairs reordered. New `tests/migration-replayable.test.cjs` pins six properties — unique parseable versions, no forward table references, no backslash-escaped quotes, no lone `$` where a dollar-quote tag belongs, and the bootstrap present, first, and still creating all ten formerly-missing tables. Every check confirmed to FAIL when violated. **Proved live**: the full history replayed into a clean staging database and finished, producing **91 tables — an identical set to production** — 377 policies, RLS on all 91, and no cron schema at all. | **Fixed** — #400 |
| GL-058 | MEDIUM | `supabase/migrations/20260721000000_tour_alerts.sql` | Four dollar-quote tags were silently halved — `DO $$` became `DO $`, `END $$;` became `END $;` — leaving SQL that cannot parse. | **Self-inflicted, by me.** Commit `c17c5f4` (#316, removing a committed secret literal) edited the file with a script whose replacement string contained `$$`. JavaScript `String.replace()` treats `$$` in a replacement as an escape for a literal `$`. The file has been unrunnable ever since and nobody noticed, because migrations are never replayed. It surfaced only during a from-scratch rebuild — and even then it first looked like a bug in the rebuild tool, because `cat -A` marks line ends with `$` and I misread `DO $` + line-marker as `DO $$`. | Restored the four tags, and `migration-replayable.test.cjs` now fails on a lone `$` where a tag belongs. Root cause worth remembering separately: a script that edits SQL must not use `String.replace()` with a computed replacement. | **Fixed** — #400 |
| GL-059 | BLOCKER | `stripe-webhook`, `invoices`, `invoice_payments` | **Invoice paid state was written directly, and every Stripe redelivery re-applied it.** The webhook PATCHed `status='paid'`; Stripe retries an event for up to three days. No transaction, no idempotency key, no ledger. The refund path PATCHed `paid_at`/`paid_amount` to null and logged its HTTP status without checking it. `invoice_payments` existed but was decorative — 19 invoices, 15 paid, **4 ledger rows** — so `invoices.status` was the system of record and the ledger agreed with it by coincidence. The partial-payments screen inserted a row and then, separately, marked the invoice paid: the audit's original GL-024. | Graded CRITICAL and release-blocking by the external auditor's follow-up. Latent rather than live: **production has never had a Stripe-paid invoice** (`stripe_session_id` is null on all 19), so no double-application has actually occurred. That is luck about traffic, not a control. | Four RPCs, each doing its work in one locked transaction, plus a trigger on `invoices` refusing any write to `status='paid'`, `paid_at`, `paid_amount`, `paid_method` or `stripe_session_id` from outside them — a browser UPDATE, a psql session and a service-role PATCH are all refused identically with 42501. A second trigger makes `invoice_payments` append-only; corrections are reversal rows. **Three things the database caught that review had not:** the existing `check (amount > 0)` made a reversing entry impossible; `method` is constrained to a six-value vocabulary the accounting screen renders from a `<select>`; and 15 invoices were paid with only 4 covered, so deriving status without a backfill would have **silently un-paid eleven real invoices**. Previewed against production first — 4 already covered and skipped, 11 backfilled and labelled, nothing flipped. | **Fixed** — #403, migration `20260831000000`, `stripe-webhook` v48. `scripts/payment-ledger-proof.sql` runs 23 live assertions and rolls back: **23/23 against production**. |
| GL-060 | LOW | `public.invoices` — `GL-1011` | `amount` is stored as `253.08999999999997`. A browser float was written into a `numeric` column without rounding. Its ledger row carries `253.09`, because `invoice_payments.amount` has a scale of 2. | None today: the ledger total is *greater* than the amount, so `paid_total >= amount` holds and the invoice stays correctly paid. Had the rounding gone the other way the invoice would have flipped to pending on the first recompute — which is the real finding, since nothing rounds on the way in. | Not corrected here. Editing a live invoice amount to make a comparison tidy is the wrong instinct: the value is what was invoiced, and the defect is the missing rounding at the point of entry. Logged so the next person meets it deliberately rather than by surprise. | **Open** |
| GL-061 | BLOCKER | payment ledger | The auditor found four defects in the ledger shipped the same day. The guard trusted `current_setting('gl.payment_apply')`, a custom GUC any SQL-capable caller can set — **reproduced: an invoice with zero ledger rows was driven to `paid / 9999` by two lines of SQL**. Each event was compared with the invoice total rather than the remaining balance, so two distinct $700 events both passed on a $1,000 invoice. Refunds were uncapped. Partial refunds nulled `paid_amount`, discarding a real balance. | The response document had asserted the exact property the guard did not have: that a psql session and a service-role PATCH were refused identically. | The guard now checks the write against the LEDGER — `paid` requires rows that cover the invoice, `paid_amount` must equal the net, `stripe_session_id` must match a real event. No flag, no privilege to borrow. Bounds moved into a BEFORE INSERT trigger on the ledger so they hold for a direct INSERT too, proven with 23514. Limiter allowance removed: a Map in one Deno isolate was never a cluster-wide bound. | **Fixed** — #406, `20260831120000` |
| GL-062 | HIGH | payment ledger | The replacement guard returned early whenever the five PAYMENT columns were unchanged, and returned **before reading the ledger**. `amount`, `invoice_number` and `is_credit_memo` were not in that condition, so an ordinary invoice edit skipped the invariant. Reproduced: raising a paid invoice from 1000 to 2000 left `status=paid` with 1000 of cover; lowering it to 500 left the ledger above the invoice. | Second bypass of the same control in one day. Each version passed its own tests. | Early return covers all eight relevant columns; the invariant is stated once in `gl_invoice_derived_status()`, called by the projection trigger, the guard AND the migration reconciliation, so migration-time and runtime cannot diverge. Both directions enforced. A tolerance of half a cent exists because `GL-1011.amount` is a browser float (`253.08999999999997`) whose ledger row rounds to `253.09` — without it that invoice is permanently unwritable. | **Fixed** — #407, `20260831180000` |
| GL-063 | MEDIUM | 13 write sites | Fifteen unchecked mutations were reported; **two were not database writes at all** — `booking-review.js` matched `URLSearchParams.delete()` and `public-ops.js` matched a comment. Of the thirteen real ones, seven asserted success to a person: commission marked paid, a backfill that set its `_migrated` flag whether or not the write landed, the `pa_letter_on_file` compliance flag, a user-ticked checkbox, vendor food-safety fields. | The backfill is the GL-001 shape exactly: a one-shot migration recording a success it did not have, and losing the data it was migrating. | Seven corrected; five documented as accepted risk with reasons in the code. `tests/checked-mutations.test.cjs` carries an allowlist with a justification per entry and fails if an exemption outlives its site — confirmed to fail on an injected regression. Its first version used a hand-rolled comment stripper that desynced on a regex containing a quote, so the walker `csp.test.cjs` already had is now shared as `tests/_jsscan.cjs` with twelve fixtures. | **Fixed** — #408 |
| GL-064 | HIGH | RLS coverage | The auditor accepted constrain-don't-rewrite but rejected the evidence: "policy counts do not prove complete effective coverage". Reporting 318 restrictive policies said nothing about whether the right ones line up with the 228 unconditional permissive ones. | A structural argument was being offered where a per-(table, command, role) one was needed. | `scripts/rls-coverage.sql` asks directly; 10 combinations have no restrictive companion and each is allowlisted with a reason and a probe result. `scripts/check-rls-coverage.sh` runs in CI against PRODUCTION and fails on anything new or on a stale entry. `scripts/rls-probe.sql` adds the behavioural half: **16/16 against production**, anon and a self-registered stranger both refused on every client-bearing table. The probe found two defects in itself first — an `insert…select` that inserted zero rows and was scored as a vulnerability, and a `serial` whose sequence permission error was scored as a PASS. | **Fixed** — #409 |
| GL-065 | MEDIUM | `contact_submissions`, 7 other tables | **Production is missing 13 columns the committed migration history creates.** `contact_submissions` has 10 of 16; seven other tables each lack `updated_at`. Cause: the migrations use `create table if not exists`, and those tables already existed in production when the migration first ran, so the statement did nothing and the columns were never added. | GL-055 in reverse — that was *production has tables the repo never creates*; this is *the repo creates columns production has never had*. Nothing is losing data today (no application code writes the missing fields), but a feature written against the repository schema would fail against production, and a restore is not a faithful reproduction. | Not corrected. Adding columns to a live table is a schema change that should be made deliberately, not folded into an audit response. Recorded in `docs/database/upgrade-replay-2026-08-31.md` with the full list. | **Open** |
| GL-066 | HIGH | 6 functions | **`gl_send_quote_decks`, `trigger_estimate_deal_value` and the four `trigger_notify_*` functions are `SECURITY DEFINER` in production and would rebuild as `SECURITY INVOKER`.** Found by fingerprinting production against a full replay of the history. | `SECURITY DEFINER` is the difference between running as the caller and as the owner. The `trigger_notify_*` functions post to an Edge Function using a Vault secret; as INVOKER they would run as whoever inserted the row — an anonymous form submitter, for the tour and quote triggers — and could not read it. Production is probably the correct state; the repository is wrong either way, because a restore from it produces six functions with different privilege semantics. `check-db-drift.sh` names this exact function in its own header as a past privilege-escalation path. | Not corrected. A migration setting six functions `SECURITY DEFINER` is a privilege change and needs the owner's decision rather than an autonomous one. | **Open** |
| GL-069 | HIGH | `accounting.js`, `billing-admin.js`, `crm-extras.js` | **Three invoice status values the code writes cannot exist.** `invoices_status_check` admits only `draft`, `pending`, `paid`, `overdue`. `accounting.js:124` writes `'voided'`, `billing-admin.js:130` writes `'expired'`, `crm-extras.js:441` writes `'quote'`. Verified against production inside a rollback: all three are **REJECTED 23514** by the constraint. | **Voiding an invoice, expiring a quote, and converting a quote back to a lead have never worked.** The void path checks its row count and reports the failure, so it surfaces as an error rather than a silent loss — but the feature is simply absent, and has been for as long as the constraint has existed. Found while writing the payment projection, which had to be built against the real status vocabulary rather than the one the code assumes. | Not corrected. Two defensible fixes — widen the constraint to admit the three values, or change the three call sites to use a status that exists — and they imply different product decisions about what a voided invoice IS. That is the owner's call, not a detail to settle inside an audit response. | **Open** |
| GL-070 | HIGH | `scripts/security-invariants.sh` | **A gateway timeout was reported as a data leak.** The daily run of 2026-09-13 (run 34760841903, attempt 1) failed with `FAIL deals — ANONYMOUS DATA VISIBLE: {"message":"Gateway Timeout"}` and the same for `profiles`. Nothing was exposed: the API answered 504 after five seconds and the request never reached Postgres. Every probe classified two ways — a response it recognised as a refusal, and *a leak* for everything else — so a timeout, a 404, a reset connection and a genuine `[{...}]` of client rows all read identically. Section 2b had the same defect pointing the **other** way: a 504 body carries no client-identifying column names either, so it PASSED. | The alarm that guards the 2026-08-06 hole cried wolf, and the run was re-run until it went green — training exactly the reflex that let the original hole live for months. This script's own section 4 already warned that "a check that cries wolf gets ignored". A leak alert nobody believes is worth less than no alert. | Every probe now has **three** outcomes, not two: `ok`, `FAIL` (tested and violated), `UNVERIFIED` (could not be tested). A leak is only ever claimed on a **2xx carrying rows**; transport failures (000/5xx/408/429) are retried 3x and then reported UNVERIFIED; anything else — a 404 from a renamed table, an auth error of another shape — is UNVERIFIED too. UNVERIFIED still exits 1, so an untested invariant never rides a green check, and the summary distinguishes "NOT PROVEN" from "SECURITY INVARIANT VIOLATED". `tests/security-invariants-classifier.test.cjs` runs the real script against a stub answering the way production did that morning, and covers all three verdicts in both directions. | **Fixed** — this PR |

| GL-077 | HIGH | `storage.objects` — `client-docs customer read` | **`client_visible` hid the row and not the file.** Phase 1 classified six documents internal and the portal honoured it. The storage policy admitted any object whose name began with the caller's own client id (`name like current_customer_client_id()::text \|\| '/%'`), and staff upload client documents to `<client_id>/docs/...`. Listing is governed by that same SELECT policy, so a customer did not even need to guess a path — they could enumerate their own prefix and sign a URL for anything in it. Verified live as `mehdy@atlascanning.com`, a real active portal owner: **the portal listed 0 documents and storage returned both hidden files.** | Not a cross-tenant leak — another client's objects were refused, then and now — but a **visibility bypass inside the tenant**, which defeats the only control standing between a client and a `Formula`-type document. The two files exposed were the Non-circumvention agreement and the Pre-Paid Canning Agreement, the exact documents the owner had asked to hold back pending his own read. This is the house mistake in its third costume: a rule enforced where the UI reads rather than where the bytes live. Omitting a column did not hide a column; a `security_invoker` view did not hide a column; hiding a row does not hide a file. | `20260915150000_storage_visibility_fix.sql` replaces the blanket prefix with four branches that each name why an object is readable: the customer's own `/portal/` uploads, `deal_documents` **with `client_visible = true`**, their own unarchived `client_artwork`, and `lot_documents`. Re-probed as four identities: Atlas 2→**0**, PERICO 4→**3** (all artwork kept), QA 1→1, staff unchanged at 39, cross-tenant and stranger 0. `tests/portal-tenant-isolation.test.cjs` §11 pins it, and was confirmed to **fail** against the old policy before being accepted. One orphaned object (`4744a07c-.../compliance/pa_letter_*.pdf`, in no table) loses customer access; no portal surface ever linked to it. | **Fixed** — this PR |
| GL-078 | MEDIUM | `public.admin_set_user_password` | **A deactivated admin kept the password-reset key.** The function can set **any** user's password — other admins and every portal customer — and gated it on `role` alone: `select role into caller_role from public.profiles where id = auth.uid()`. `profiles.status` was never read, and deactivation sets status, not role. | The GL-009 shape: a permission check that reads one half of the identity. **Not exploitable today** — all six profiles are `status = 'active'`, so no identity currently sits in the gap — which is the only reason this is a register entry and not an incident. A revoked administrator would have retained the ability to take over the accounts of the people who revoked them. | `20260915150100_admin_password_status_check.sql` adds `and coalesce(status,'active') = 'active'` to the caller lookup. Could not simulate the deactivated admin: `profiles_block_sensitive_updates` refuses a status change from anything but a super-user, and disabling a production security control to demonstrate a bug visible in six lines of source is a bad trade. Verified the other way instead — an **active** admin still passes the gate (reaches `user not found` on a nonexistent target), a portal customer and a stranger both get `admin only`. Two further weaknesses are **reported, not changed**, because they alter behaviour staff may rely on: the minimum is 6 characters (weaker than `onboarding-set-password`, and Supabase leaked-password protection is disabled on this project), and the function writes no `audit_log` row, so an account takeover leaves no trace. | **Fixed** — this PR |

| GL-079 | MEDIUM | Vercel deployment | **The entire repository was served on the public web.** Vercel serves from the repo root and there was no `.vercelignore`, so every committed file was a URL. Confirmed live on 2026-09-16, all returning 200 with real content: `/docs/database/authorization-baseline.txt` (113 KB — every RLS policy expression, table grant, and SECURITY DEFINER function with its exact signature and ACL), `/docs/plans/technical-debt.md` (69 KB — **this register, including the issues still open**), `/CLAUDE.md`, `/SECURITY.md`, `/CRM_FEATURE_MAP.md`, `/scripts/security-invariants.sh`, `/scripts/db-drift-snapshot.sql`, `/supabase/migrations/*.sql`, `/supabase/functions/**/*.ts`, `/supabase/config.toml`, `/tests/*.test.cjs`, `/prompts/`, `/.github/workflows/`. Found by probing the deployed site for sensitive paths during the full-site smoke test, not by reading the repo. | **No credentials were exposed**, and that is the difference between this and an incident: `.gitignore` keeps `supabase/secrets.env` and every `.env` out of the repository, so they were never uploaded — `/supabase/secrets.env` returned 404, and a scan of all 95 shipped scripts found no key material beyond the publishable key, which is meant to be public. RLS is the enforcement and RLS holds: an anonymous visitor was refused on 29 of 30 tables, on every write, and on every portal RPC. So this is reconnaissance, not a breach — but it handed an attacker the exact function inventory to probe, the migration history to diff, and a register naming the weaknesses we already know about and have not fixed. Obscurity is not the defence; there is still no reason to publish the map. | `.vercelignore` excludes `docs/`, `scripts/`, `tests/`, `tools/`, `prompts/`, `supabase/`, `.github/`, `.claude/` and `*.md`. Verified before writing that nothing shipped depends on them: all six HTML entry points reference 112 assets, **0** of which are excluded. `tests/published-surface.test.cjs` guards both halves — those paths stay out, and nothing the pages load is excluded with them — and was confirmed to fail in both directions (15 failures with the rules removed; the site-breaking bare `supabase` without its slash caught too). Wired into `smoke-test.yml`. **Takes effect on the next deploy**; re-probe `curl -o /dev/null -w '%{http_code}' https://www.goodliquidbevco.com/CLAUDE.md` after it lands and expect 404. | **Fixed** — this PR |
| GL-080 | MEDIUM | `public.quotes` → `project_entitlement_events` | **An accepted quote did not unlock what it sold.** Phase 1 built the locked portal tabs; granting one stayed a manual tick in project admin, so a client could keep reading a sales pitch for a service already paid for. The plan's "quote → entitlement wiring" also assumed more than the schema had: `quotes.status` admitted `accepted` but no quote had ever left `draft`; `quotes.addons` holds production options (nitrogen, trays, palletizing), not portal services; and entitlements are per project while quotes carried only a client. | Revenue-adjacent: the portal's locked tabs are the sales surface, and a bought service that stays locked undermines it and generates support. | `20260916000000_quote_entitlement_wiring.sql`: `quotes.project_id` (composite tenant FK + needs-client check) and `quotes.services text[]` (constrained to the three keys), set explicitly by staff rather than inferred from line-item wording. `trg_quote_grant_entitlements` fires only on the transition into `accepted`, is idempotent against the ledger, grants nothing without a project or on an archived one, and emails the client once. **The first version failed its own rollback probe** (23502): the ledger's `actor` is NOT NULL and `auth.uid()` is null outside a browser session; rather than weaken a deliberate column it now raises 42501 with no signed-in actor. Verified end-to-end through the real staff UI on the QA client: 2 grants, 1 email, 2 audit rows naming the staff user, and the customer RPC reporting all three tabs unlocked. Renders and Market Analytics tabs now list published `Product Render` / `Market Analysis` documents through the same `client_visible` path as every other document. Guarded by `tests/portal-tenant-isolation.test.cjs` §13. | **Fixed** — `fe675a2` |
| GL-081 | MEDIUM | `src/shared/help-features.js`, `src/shared/ai-hub.js` | **The sidebar "AI Tools" button did nothing.** The dispatcher runs `glAIHubTogglePanel` from a listener on `document`; `help-features.js` registers the panel's close-on-outside-click handler on `document` too, exempting only the panel and `#ai-toolbar`. One click ran both: the toggle opened the panel and the closer shut it on the same event. Verified live — calling the function directly moved `display` none → flex, clicking the button left it at none. The floating FAB was unaffected because it sits inside `#ai-toolbar` and calls `stopPropagation`. | A dead button with no console error and no failed request — the exact shape `actions.js` warns about, and invisible to any handler-exists check. Found only by clicking it. | The closer now exempts any element whose `data-gl-action` is `glAIHubTogglePanel`, matched by action name rather than id so the next control wired to the toggle is covered. Verified on production: opens on click, closes on second click, still closes on an outside click. Guarded by `tests/dead-controls.test.cjs`. | **Fixed** — `51704cc` |
| GL-082 | LOW | `src/modules/quotes/quote-builder.js` | **The quote Services editor was permanently expanded.** Introduced in GL-080 the same morning: the panel carried both a `hidden` attribute and an inline `display:flex`, and inline display outranks the UA stylesheet's `[hidden]{display:none}`. The toggle flipped `hidden` with no visible effect. | Cosmetic but misleading: every quote row showed an open editor, and the control that should open it appeared broken. | The panel starts `display:none` and the toggle drives `display`, the property it was initialised with. No other element in shipped source carries both `hidden` and an inline display. Verified live: 0px on load, 147px open, 0px closed. Guarded by `tests/dead-controls.test.cjs`. | **Fixed** — `51704cc` |
| GL-083 | MEDIUM | `src/modules/quotes/quote-builder.js` | **Re-saving a quote in the builder reset its status to Draft.** The builder's save payload hardcoded `status: 'draft'` and used it for updates as well as inserts. Harmless until GL-080 made status meaningful: accept a quote, reopen it to fix a typo, save, and the record reverts to Draft while the services its acceptance unlocked stay unlocked, because grants are ledger events. | The quote record contradicts the entitlement ledger, and a later re-accept would look like a fresh acceptance. No exposure, but the billing-adjacent record lies. | `status` is now set on insert only; updates leave it to the Services control. Guarded by a check in `tests/portal-tenant-isolation.test.cjs` §13, confirmed to fail with the old payload restored. | **Fixed** — this PR |
| GL-084 | MEDIUM | `src/shared/help-features.js` (invoices realtime) | **275 "Maximum call stack size exceeded" errors were silenced, not fixed.** `error_log` held them from 7 Aug to 3 Sep, admin and portal alike, every stack inside supabase.min.js channel code. GL-046 read it as a library bug and added a silent `.catch()`; the log went quiet days later and looked fixed. The recursion was in the app: the status callback called `removeChannel(channel)` on CLOSED, and removing a channel emits CLOSED to that same callback. | A tight recursion on every realtime drop — CPU burned until the stack blew, and in a live reproduction a frozen tab. Hidden because the error was swallowed. | Reproduced live against production first: one external close fired the callback **52 times and called removeChannel 50 times** before a safety cap; without the cap the tab froze. Fix: a per-channel `closing` flag that ignores the CLOSED our own removal emits, and the removal deferred out of the callback with `setTimeout`. Same reproduction afterwards: **3 callbacks, 1 removal**, channel cleaned up. `tests/realtime-channels.test.cjs` checks every status callback that removes a channel for both parts; confirmed to fail against the previous code. | **Fixed** — this PR |
| GL-085 | HIGH | `crm-index-core.js` (deal panel), `index.html` | **Editing a deal showed blank notes and Save discarded new ones.** The deal panel's Meeting Notes container was `id="ddp-notes"`, the same id as the notes `<textarea>` in the edit form, and the view panel comes first in the document. `editDealDetail()` wrote the existing notes onto the `<div>` as an expando and left the textarea empty; `saveDealDetail()` read them back off the `<div>`. When the view re-rendered between Edit and Save the fresh `<div>` had no `.value` and `.trim()` threw — the 4 "reading 'trim'" rows in `error_log` (21 Aug – 2 Sep). | Silent data loss on the pipeline's main edit form: typed notes vanished with no error, and a returning user saw an empty notes box on deals that had notes. | Proven live on a real deal before fixing, without saving: notes present on the deal, textarea blank, notes stashed on the div, save would have written the old text back. Renamed the container to `ddp-meeting-notes` (two references) rather than the field, which four call sites read. Guarded by `tests/dead-controls.test.cjs` §3, which fails on any form-field id defined in more than one file; against the previous code it reports exactly this and GL-086. | **Fixed** — this PR |
| GL-086 | HIGH | `src/modules/production/trace.js`, `compliance.js` | **The Trace/Recall search could run for an empty string.** Its box was `id="gl-trace-q"`, the same id as the lot input on Compliance's "Trace Lot" tab. Both pages stay in the DOM and Compliance comes first, so once anyone had opened that tab, `traceQuery()` read Compliance's box instead. | A recall tool that silently searches for nothing is the failure that matters most during an actual recall or mock recall. | Proven live: opened Compliance → Trace Lot, typed a run name on Trace/Recall, and the search received `""`. Renamed the Trace/Recall box to `gl-recall-q` (three references, all in trace.js); the Compliance id stays because its QR scan, inventory 🔍 Trace button and lot-sticker hook use it. Same guard as GL-085. A full scan of 1,495 literal ids found 12 defined in multiple files; the rest were checked and are created-on-demand modals, scoped lookups, or have no observed collision. | **Fixed** — this PR |
| GL-087 | LOW | `src/modules/invoicing/billing-admin.js` | **Every public visitor downloaded the backup exporter's zip library.** `ensureJsZip()` ran at load for everyone — public site, portal, booking links — to warm up a staff-only "Export everything" button, and its rejection was never handled. When a blocker stopped the request, that became an unhandled promise: 100+ "jszip load failed" rows in `error_log`, mostly with no actor. | 97 KB of wasted download per anonymous page view, and log noise that buries real errors. The export itself was never broken: the pinned SRI hash matches what cdnjs serves, CORS allows it, and the click path awaits inside a `try` and retries after a failed load. | Warm-up now waits for a signed-in staff session (polled, since sign-in happens without a reload) and swallows its own failure. Verified on production after deploy. | **Fixed** — this PR |
| GL-088 | MEDIUM | `src/services/auth.js` | **An expired session looked like a broken CRM.** On 2026-09-15 the owner reported most of the admin area broken: empty dashboard, empty pipeline. Nothing was broken — the refresh token had failed (400 on `/auth/v1/token`), every query returned 401, and the staff shell kept rendering as though signed in, with no data and no message. `error_log` was 401-blocked too, so nothing recorded it. | Hours of misdiagnosis, and a real risk that someone keeps typing into forms that can no longer save. | A watcher shows a banner — "Your session has expired, so nothing will load or save" — with a Sign in again button, when a staff shell is open but there is no valid session. It distinguishes expiry from deliberate sign-out because every intentional path clears `window.currentUser` after `signOut` and an expiry does not; it checks on `SIGNED_OUT`, every 60 s, and when the tab becomes visible. It does not auto-reload, so a half-typed form can be copied first. Verified on production by stubbing `getSession` in the page, which does not touch the real session. | **Fixed** — this PR |
| GL-089 | HIGH | production-runs.js, quality.js, public-ops.js, tools.js | **Eight create forms showed records that were never saved.** Production runs, defects/NCRs, vendors, formulas (and formula clones), yield logs, sample shipments, trade shows and content posts: when the insert failed, each invented a `local_` id, added the record to the list, kept it in localStorage and — for five of them — announced success. Nothing ever uploads those rows later. The formula clone also closed its form before attempting the save, so a failure lost what was entered. | A supplier shown as approved, an NCR shown as logged, or a yield figure shown as recorded that exists on one device only — records an auditor, a colleague or the next page load will not find. Found by filling the Add Vendor form with database writes intercepted: the vendor appeared in the list after a save that never happened. | All eight now create through `glCheckedInsert` and stop on failure: report it, keep the form open, add nothing — the rule `tests/checked-inserts.test.cjs` already applied to deals, clients and referrals. Updates to real rows keep their error and zero-row checks, and a thrown error no longer falls through to a local-only edit. Existing `local_` rows already on a device can still be edited locally, since they have no database row. The test gains 17 checks, all confirmed to fail against the previous code. | **Fixed** — this PR |
| GL-090 | HIGH | `src/modules/production/compliance.js` | **A compliance record the database rejected was reported as signed and saved.** `dbInsert` keeps a rejected record locally on purpose — flagged `_localOnly`, with a "DB save FAILED" warning and a re-save path in `cip-audit.js` — so a signed FDA log is not lost. The save routine then treated that local copy as real: `audit_log` recorded `compliance_record_saved` and the operator saw "✓ Signed: GMP-SAN-002". Found by filling Log Cycle with database writes intercepted. | The audit trail claimed a record existed in the system of record when it existed on one device only — the claim an inspector would rely on. | A local-only record is now audited as `compliance_record_saved_local_only` with the database error, and announced as "⚠ NOT saved to the database". The keep-locally design is unchanged. **Left for the owner:** on a failed save the automatic hold tag and NCR are still created, pointing at the record's temporary `tmp_` id. Raising the hold regardless is arguably right for food safety, but those rows reference a record that does not exist until re-saved, and re-saving does not relink them. A second instance in `spawnHoldAndNc` announced "🚫 Hold Tag created" after the hold tag itself was rejected; it now says the hold is NOT saved and that product is not on hold for anyone else until re-saved. Its auto-NCR also read "Auto-generated from compliance record ." with a blank id when a hold was raised directly; it now names the hold tag instead. Guarded by three checks in `checked-inserts.test.cjs`, confirmed to fail on the previous code. | **Fixed** (claim) / **Open** (tmp_ references) — this PR |
| GL-091 | HIGH | `src/modules/production/gmp.js` | **"Log today's GMP" opened an empty form.** The action dispatcher appends the click Event as the last argument, so a button with no `data-gl-arg1` called `glOpenDailyGMP(onlyCode)` with `onlyCode = MouseEvent`. That is truthy, so the screen looked up a form whose code was the event object and rendered "Form [object MouseEvent] is not set up yet." Both the green "➕ Log today's GMP" button and every daily register's own Log button were broken; only the tile worked, because it happened to pass an empty string. | Operators could not log daily GMP from the page's main button — the primary way FDA daily records get entered. No console error, no failed request. | A form code must be a string; anything else now means all of today's forms. Found by clicking the button with writes intercepted, then proved by calling the function directly (49 fields) versus with a click Event (0). A scan of all 156 actions reachable without an argument found one other handler with a non-event first parameter, `glOpenAddHoldTag(prefill)` — harmless today but hardened the same way. `tests/dead-controls.test.cjs` §4 fails on any such handler that does not reject the Event, and named exactly these two against the previous code. | **Fixed** — this PR |
| GL-092 | MEDIUM | `src/modules/pipeline/deal-detail.js` | **A rejected deal edit stayed on the board.** `saveDealDetail` writes every edited field onto the in-memory deal before calling the database. When the update was rejected it rolled back the stage move only, then alerted "Nothing was saved and the deal has NOT been moved" — while the board went on showing the unsaved name, company, value, probability and notes until the next reload quietly reverted them. | The screen contradicted the alert on the pipeline's main edit form; a user who dismissed the alert could reasonably believe the changes had stuck. | The deal is snapshotted before any field is changed and restored in place on failure (same object, so the board and `deals[]` references stay valid). Found by saving a deal edit with the database write intercepted — the same run confirmed GL-085 end to end: Edit showed the real notes, and the typed note reached the update payload with the originals kept. Guarded by two ordering checks in `checked-inserts.test.cjs`, confirmed to fail on the previous code. | **Fixed** — this PR |
| GL-093 | HIGH | `src/modules/customers/edit-client.js` | **A rejected client edit was called "Saved locally", and on a network error the form closed as if it had saved.** `glUpdateClient` applies the edit to the in-memory client first. On a database error or a zero-row update it notified "Saved locally" and returned false — but nothing keeps local edits: clients reload from the database on every page load, so they vanished on refresh. The network-error branch was worse: it notified "Saved locally / Server unreachable" and did **not** return false, so it fell through, audited `client_edited`, returned success, and the form closed. The form's own message was "Save failed — check the browser console." | The primary client record form could report success, or a reassuring "saved locally", for edits that were about to disappear; and `audit_log` could record an edit that never happened. | Every failure path now restores the client in place and returns false with the real reason, shown in the form as "Not saved — … Your edits are still in this form." and notified as "Client NOT saved". Found by saving an Edit Client change on the internal QA client with the database write intercepted: all six edited values reached the update payload, and the local client kept all six after the failure. Guarded by two checks in `checked-inserts.test.cjs`, confirmed to fail on the previous code. | **Fixed** — this PR |
| GL-094 | HIGH | `crm-index-core.js` (moveDeal, setDealOutreach, saveNewClient), `src/services/integrations.js` | **Three writes never looked at their result (CLAUDE.md rule 4).** `moveDeal` moved the card to its new pipeline column and saved without `.select()` or an error check — a rejected move stayed on the board with no message, and on a move to **Closed Won the deal-won notification and the SMS wrapper fired regardless**. `setDealOutreach` kept a rejected outreach status on the card and still logged "Email logged" to the activity feed. `saveNewClient` recorded each uploaded W-9, tax-exempt certificate and PA letter path unchecked, so a rejected update left the file in storage while the client record said nothing was on file. | Deal-won texts and emails for a deal that did not close; pipeline status the database never accepted; compliance documents that were uploaded but invisible. None surfaced an error. | Each now appends `.select()` and treats an error or zero rows as failure: the move and the outreach status revert with a message, `moveDeal` returns false and the SMS wrapper checks it, and an unlinked document raises "Document not linked — re-attach it from Edit Client". Found by clicking a pipeline move button with the database write intercepted, then scanning every shipped `.update(` / `.delete(`. New `tests/unchecked-writes.test.cjs` fails on any such write that is not allowlisted with a reason — eight are, each explained (two are comments, one is checked on the next statement, five are background cache bookkeeping) — and on stale allowlist entries. Confirmed to fail on the previous code. | **Fixed** — this PR |
| GL-095 | HIGH | `compliance.js`, `onboarding.js`, `crm-index-core.js` | **Four inserts reported success after the database rejected them, and a dormant wrapper would have duplicated every client.** An RLS-rejected insert returns an error rather than throwing, and these ignored the result: (1) a **glass-breakage** event created its physical-hazard hold tag unchecked and announced "Hold Tag HT-… created" regardless; (2) the **CSV training import** incremented its counter after every insert and toasted "Imported N training records" even if all were rejected; (3) a **weekly-review sign-off** upserted unchecked and was cached as signed (the un-sign path already checked); (4) **label artwork** carried over at deal conversion was inserted unchecked while the PA letter beside it reported failures. Separately, a `window.saveClient` wrapper re-inserted the last client unchecked; no `saveClient` exists so it never ran, but `saveNewClient` already inserts, so defining one would have silently created every client twice. | Product believed to be on hold after a glass breakage when it is not; FDA training and weekly review records reported as recorded when they are not; artwork silently missing from a new client. | Each now reads the result and treats an error or no returned row as failure: the glass-breakage path alerts that the hold tag was NOT created and to create it manually; the import reports "Imported X of N" and lists the rows not saved; the sign-off is cached only once recorded; the artwork failure joins the existing carry-over problem list. The dead wrapper is removed. Found by scanning every shipped insert/upsert whose result is discarded (22, of which three were false positives and about ten are background logs). Guarded by five checks in `unchecked-writes.test.cjs`, all confirmed to fail on the previous code. | **Fixed** — this PR |
| GL-096 | HIGH | `src/modules/invoicing/invoice-patches.js`, `invoice-builder.js`, `crm-extras.js`, `crm-index-core.js` | **Invoices were saved without their add-ons or notes, and announced as saved before the database was asked.** (1) The save, the builder's displayed total and the edit prefill all found add-on inputs with `input[oninput*="addons"]`; those inline handlers became `data-gl-action` in GL-DEF-01, so the selector matched nothing and every add-on — nitrogen dosing, trays, palletizing — was dropped from the saved amount and line items. **Invoices under-billed by exactly the add-ons**, and the builder's own total agreed with the wrong figure, so nothing looked off. (2) Notes / payment instructions were hard-coded to `''` on the local copy and the insert, though they print on the PDF, the public invoice link and invoice detail. (3) "Invoice saved" was toasted, the builder closed and `invoice_save` was audited before the insert; a rejection then only said "Cloud sync failed — saved locally only" about an invoice that existed in memory until reload. | Lost revenue on every invoice with add-ons; customer-facing payment terms never delivered; an audit log and a screen claiming invoices that did not exist. | Add-ons are read by their `data-gl-action` inputs and paired by row index in the save, the live total and the edit prefill; notes are saved on insert and update; the toast says "Saving…" and "Invoice saved" plus the audit happen only once the database returns the row. A rejected save removes the phantom (restoring the prior copy for an edit), reopens the builder with the data and edit markers intact so a retry updates rather than duplicates, and alerts. Notes are escaped in both PDF templates and the builder's own inputs, since they now carry real content. Found by saving a builder invoice with the database write intercepted: displayed $376.50 and sent $338.85 for a form that also had a $40 add-on and notes. Six checks in `unchecked-writes.test.cjs`, confirmed to fail on the previous code. | **Fixed** — this PR |
| GL-097 | MEDIUM | `crm-index-core.js`, `src/modules/invoicing/accounting.js`, `src/shared/crm-extras.js` | **Three client-panel buttons silently disappeared in the inline-handler conversion.** The 📝 Notes and 📧 Templates buttons were injected next to `[onclick*="aiScoreClientHealth"]`, and 📄 Statement (statement of account) found `button[onclick*="glOpenEditClient"]` and parsed the client id out of the handler text. Neither attribute exists any more, so none of the three appeared, though `openClientNote`, `openEmailTemplates` and `glOpenStatement` all still exist. Two sidebar-highlight lookups had the same shape (cosmetic). Confirmed on production before fixing: the client detail panel showed none of the three. | Features the owner had built stopped being reachable with no error. | Selectors now target the `data-gl-action` controls and read the id from `data-gl-arg1`. `tests/dead-controls.test.cjs` §5 fails on any code that selects elements by an inline event-handler attribute — with the inline-handler budget at 0 such a selector can never match — and flagged all of these, plus GL-096, against the previous code. **Follow-up:** Statement still did not appear live — the client overlay is appended to `document.body` and the accounting observer only watched `#crm-panel`, so opening a client never ran the injector. It now also observes body’s direct children (§6 of the same test). Verified on production: all three buttons appear on open, and Statement opens the statement of account. | **Fixed** — this PR |
| GL-098 | MEDIUM | `src/modules/production/compliance.js`, `src/shared/tools.js` | **Three more writes discarded their result and then reported success.** The CIP equipment editor upserted each name without looking at the result, then cached the list locally and closed as saved. The annual FSP reminder announced “Annual FSP review scheduled” whether or not the task row was inserted. The customer NPS survey thanked the customer even if the response was rejected. Found by a scan for `insert`/`upsert` statements whose result is neither assigned nor chained. The remaining hits are logs of something that already happened (`followup_log`, `email_log`, `login_events`, `contact_submissions` alongside the quote RPC) or AI cache (`ai_briefs`) and were left as they are. | Silent data loss on a food-safety equipment list and on customer feedback. | Each result is checked; failures keep the user’s input and say so. The NPS insert deliberately checks only `error` — it runs as the anonymous role, which may insert but not read `nps_responses`, so `.select()` would fail every submission. Guarded in `tests/unchecked-writes.test.cjs`. | **Fixed** — this PR |
| GL-099 | HIGH | `src/modules/invoicing/invoice-patches.js`, `supabase/migrations/20260916120000_invoice_amount_cents.sql` | **Invoices saved with floating-point amounts could not be paid in full.** The builder computed `subtotal - discount` in JavaScript floats and wrote the result to an unconstrained `numeric` column. Production held GL-1030 (pending, created 2026-09-16) at `6491.999999999999` and GL-1011 at `253.08999999999997`. The payment ledger is `numeric(12,2)` and refuses a payment that takes an invoice past its amount, so a $6,492.00 payment is rejected as an overpayment and $6,491.99 never marks it paid — proven against production with the literal values. Through Stripe, the card would be charged and the webhook’s ledger insert refused. Found while reading the audit log during the click-through. | Customer payments on affected invoices cannot be recorded; a Stripe payment would be taken but not booked. | The builder rounds to cents. A `BEFORE INSERT OR UPDATE OF amount` trigger rounds `invoices.amount` for every writer (legacy form, credit memos, late fees, edge functions), named to fire before `invoices_guard_paid_state`. Proven in a rolled-back probe: a float amount stores as 6492.00 and a full payment marks it paid. Existing rows are NOT rewritten — GL-1030 and GL-1011 are real invoices; re-saving GL-1030 corrects it, or the owner can approve `update invoices set amount = round(amount, 2) where amount <> round(amount, 2)`. Baseline: 922 facts, `d67b2232fa0bc7feaf38c3207bcc6705`. | **Fixed** for new writes; **GL-1030 needs the owner** |

Worth keeping because of how it was found: not by review, but by parsing the
register to answer "what is still open" and noticing the arithmetic did not add
up. A register nobody parses is a register that quietly rots.
