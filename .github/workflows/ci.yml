name: CI
on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Replace this placeholder with the CRM's actual build, lint, type-check, and test commands."
